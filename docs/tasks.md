# Task List

หมายเหตุลำดับการสร้าง (build order ตามที่ผู้ใช้กำหนด): Foundation → US-7 (2D validation) → US-1 (3D map) → US-2 (nav to detail) → US-4 (add entry) → US-3 (unlock animation) → US-5 (entry list) → US-6 (stats, ปิดท้าย)

ทุก task ที่มีป้าย **[รอ design-spec]** ต้องรอ UIUX ทำ docs/design-spec.md เสร็จก่อนเริ่ม ส่วน task ที่ไม่มีป้ายนี้ (scaffold, data layer, storage layer, state logic, navigation wiring, 2D validation) เริ่มได้ทันทีขนานกับงาน UIUX

หมายเหตุลำดับการสร้าง (รอบ Landmark Check-in & Supabase Integration — US-8 ถึง US-15): Foundation (sync/auth infra, ไม่ต้องรอ design-spec) → US-11 (anonymous auth) → US-15 (migrate ข้อมูล v1 เดิม) → US-8/US-9/US-10 (landmark check-in + auto check-in) → US-12/US-13 (sync entries + photo upload) → US-14 (map visual feedback). งาน data/logic layer เริ่มขนานกับ UIUX ได้ทันที ส่วนงานที่แตะหน้าจอ (ProvinceDetailScreen, AddEntryScreen, HomeScreen, Settings/Profile) ต้องรอ docs/design-spec.md ฉบับอัปเดตจาก UIUX ตามป้าย **[รอ design-spec]**

หมายเหตุลำดับการสร้าง (รอบ OSM Data Integration — US-16 ถึง US-20): T60 (เขียนสคริปต์ extraction) → T61 (รันสคริปต์จริงกับ Overpass ถ้าทำได้ในสภาพแวดล้อมนี้ ไม่งั้น fallback) → T62/T63 (ขยาย Landmark type + backfill พิกัดให้ 8 จังหวัดนำร่องเดิม) → T64 (merge ผลลัพธ์เข้า 68 จังหวัดที่เหลือ) → T65 (regression check US-8/9/10 เดิม) ควบคู่กับ T66 (Supabase landmarks schema) และ T67 (map UI, รอ design-spec) แยกอิสระคือสาย US-19/US-20: T68 (nominatim client module) → T69 (ขยาย entries schema) → T70/T71 (search UI + wiring, รอ design-spec) → T72/T73/T74 (debounce/throttle/error handling). งาน T60, T62, T63, T68, T69 เป็น data/logic layer ล้วนๆ เริ่มขนานกับ UIUX ได้ทันทีไม่ต้องรอ design-spec

หมายเหตุลำดับการสร้าง (รอบ 6: Bug Fix US-25 — แยก "ยังไม่มีข้อมูล" ออกจาก "ดึงข้อมูลล้มเหลว"): T87 (แก้ wikipediaService.ts ให้ throw/reject เมื่อล้มเหลวจริง) → T88 (error state ใหม่ใน LandmarkList.tsx แยกจาก empty state เดิม) → T89 (retry action + clear error เมื่อสำเร็จ). ไม่ต้องรอ design-spec ใหม่ (ดูคำตัดสิน PM ข้อ 18) เริ่มได้ทันทีขนานกับงานอื่น แต่ผู้ implement ต้องอ่านโค้ดปัจจุบันจริงของ `src/components/LandmarkList.tsx` ก่อนแก้ เพราะมีงาน UI redesign คู่ขนาน (Bento Grid/Shimmer/PressableScale) แก้ไฟล์เดียวกันอยู่แล้วบางส่วน

หมายเหตุลำดับการสร้าง (รอบ 7: Bottom Tab Navigation & ข่าวท่องเที่ยว RSS — US-28 ถึง US-32): **T90 (verify RSS endpoint จริง + ตัดสิน fallback source ตามคำตัดสิน PM ข้อ 19) ต้องทำเป็นอันดับแรกสุดก่อนเขียนโค้ดฝั่งข่าวใดๆ** เพราะถ้า endpoint หลักใช้ไม่ได้จะกระทบ scope ทั้งหมดของ US-29 ถึง US-32 — ทำขนานกับ T91 (ติดตั้ง dependency ที่ยังขาด: `@react-navigation/bottom-tabs`, `expo-web-browser`) ซึ่งไม่ต้องรอ T90 จากนั้นแยกเป็น **3 สายที่เสี่ยงคนละแบบ ต้องแยกจากกันชัดเจน**: (1) สาย **Navigation restructure** T92→T93→T94(design-spec)→T95→T96 — เสี่ยงสูงเพราะแตะ root navigator กระทบทั้งแอปที่ทดสอบผ่านมาแล้วทุกรอบก่อนหน้า (US-1/2/3/14/24/26), (2) สาย **News data/logic** T97→T98→T99→T100 — เป็นของใหม่ล้วนๆ ไม่กระทบโค้ดเดิม เริ่มขนานกับสาย Navigation ได้ทันทีไม่ต้องรอกัน, (3) สาย **News UI** T101-T104 ต้องรอทั้ง T100 (มี data ให้ผูก) และ docs/design-spec.md ฉบับใหม่จาก UIUX (ดูคำตัดสิน PM ข้อ 22) — ส่วนสาย **In-app browser** T105→T108 รอ T100 (ต้องมีรายการข่าว/link ก่อนถึงจะกดเปิดได้) และ T91 (ต้องมี `expo-web-browser` ติดตั้งแล้ว) แต่ไม่ต้องรอสาย Navigation เสร็จสมบูรณ์ก่อน สุดท้าย T109 (verify edge case รวมของ US-32) รอ T97-T100 เสร็จทั้งหมด งาน T90, T91, T92, T93, T95, T96, T97, T98, T99, T100, T105-T108 เป็น non-UI (verification/dependency/wiring/data/logic layer) เริ่มขนานกับ UIUX ได้ทันที มีเพียง T94, T101-T104 เท่านั้นที่ต้องรอ design-spec

หมายเหตุลำดับการสร้าง (รอบ 14: Bug Fix รูปภาพข่าว & ยกระดับ Animation/Motion — US-33, US-34): สาย **US-33** T113 (verify og:image/fallback source) → T114 (lazy og:image fetcher module, ทางเลือก (ข) เป็นหลักตามคำตัดสิน PM ข้อ 24) → T115 (wire เข้า NewsCard) → T116 (contingency: สลับไปทางเลือก (ก) เฉพาะถ้า T113 พิสูจน์ว่า (ข) ทำไม่ได้จริง) → T117 (test + รายงานตัวเลขจริง) — ไม่ต้องรอ design-spec ใหม่ใดๆ เพราะ reuse โครงสร้าง NewsCard เดิม เริ่มขนานกับสาย US-34 ได้ทันที สาย **US-34** ต้องรอ T118 (UIUX ออกแบบ motion/micro-interaction spec ใหม่ใน design-spec.md) ก่อนเสมอ จากนั้น T119/T120/T121 (screen transition / list entrance animation / press feedback) ทำขนานกันได้ ตามด้วย T122 (regression guard) และ T123 (test ยืนยัน animation ถูกเพิ่มจริง) ปิดท้าย — ทั้งสองสายไม่ผูกกัน ทำขนานกันได้เต็มที่

## P0 (ต้องมี)

### Foundation (ไม่ต้องรอ design-spec)
- [ ] T1: Scaffold โปรเจกต์ Expo + React Native + TypeScript (โครงสร้างโฟลเดอร์ src/screens, src/components, src/data, src/storage, src/navigation) — dependency: ไม่มี
- [ ] T2: Copy `thailand-provinces.ts` และ `thailand-provinces.json` จาก project root เข้า `src/data/` (ใช้ไฟล์ที่มีอยู่แล้ว ห้ามสร้าง/แก้ข้อมูลใหม่ ตาม Out of Scope) — dependency: T1
- [ ] T3: สร้าง storage layer ด้วย expo-sqlite: schema ตาราง journal entries (id, provinceId, date, title, notes, photoUris, tags) + CRUD functions พื้นฐาน — dependency: T1
- [ ] T4: ตั้งค่า navigation stack (Home → ProvinceDetail → AddEntry, และ Stats) — dependency: T1
- [ ] T5: เขียน derived-state utilities: isVisited (entries.length > 0), unlocked count, region aggregation helper (ใช้ฟิลด์ region ของแต่ละจังหวัด) — dependency: T2, T3

### US-7: 2D map validation step (ไม่ต้องรอ design-spec — เป็น dev-only, flat color ตาม AC)
- [ ] T6: Render แผนที่ 2 มิติแบบเรียบครบ 76 จังหวัด โดยใช้ path data ตรงจาก `thailand-provinces.ts` ไม่มีการ re-derive พิกัด (อ้างอิง US-7) — dependency: T2
- [ ] T7: แตะจังหวัดแล้ว toggle สถานะ visited ใน memory (เทา ↔ เขียว) พร้อมตรวจสอบ tap target ไม่ทับซ้อนจังหวัดข้างเคียงแม้จังหวัดเล็ก เช่น สมุทรสงคราม (อ้างอิง US-7) — dependency: T6

### US-1: แผนที่ 3 มิติภาพรวม
- [ ] T8: Build 3D map container (top face + side face ต่อ block, กรอบ rotateX 30–40 องศาตลอดเวลา ตามที่ US-3 AC3 ต้องการเช่นกัน) โดยใช้ path data ที่ validate แล้วจาก T6/T7 **[รอ design-spec]** (อ้างอิง US-1, US-3) — dependency: T7
- [ ] T9: ผูก locked/unlocked visual state (สี, ยกตัว/ไม่ยกตัว) เข้ากับ isVisited จาก T5 **[รอ design-spec]** (อ้างอิง US-1) — dependency: T8, T5
- [ ] T10: Header "ปลดล็อกแล้ว X / 76 จังหวัด" + progress bar อัปเดตแบบ real-time **[รอ design-spec]** (อ้างอิง US-1) — dependency: T5

### US-2: นำทางไปหน้ารายละเอียดจังหวัด
- [ ] T11: แตะจังหวัดบนแผนที่ 3 มิติแล้วนำทางไปหน้า ProvinceDetail ด้วย province.id ที่ถูกต้อง (อ้างอิง US-2) — dependency: T8, T4
- [ ] T32: [ใหม่ จากคำตัดสิน PM ข้อ 1] เพิ่ม long-press gesture บน Province Tile: กดค้างแสดง tooltip ชื่อจังหวัด (nameTh) ลอยขึ้นชั่วคราวเหนือ tile โดยยังไม่ navigate, ปล่อยนิ้ว/tap สั้นตามปกติจึง navigate ไปหน้า ProvinceDetail ตาม T11 **[รอ design-spec]** (อ้างอิง US-1, US-2) — dependency: T9, T11

### US-4: เพิ่มบันทึกทริปใหม่
- [ ] T12: ฟอร์ม AddEntry ครบทุกฟิลด์ (date, title, notes, photoUris, tags) พร้อม validation บังคับ title+date **[รอ design-spec]** (อ้างอิง US-4) — dependency: T4
  - หมายเหตุ tag field (จากคำตัดสิน PM ข้อ 3): fixed tag set เริ่มต้น 8 แท็ก (ทะเล/ภูเขา/วัด/คาเฟ่/น้ำตก/เมือง/ธรรมชาติ/อาหาร) แบบ multi-select chip + ผู้ใช้พิมพ์ custom tag เพิ่มเองได้ (text input → เพิ่มเป็น chip ใหม่ เลือกได้เหมือน fixed tag)
- [ ] T13: เชื่อม expo-image-picker เลือกได้หลายรูปต่อ entry (อ้างอิง US-4) — dependency: T12
- [ ] T14: บันทึก entry ลง expo-sqlite ผูกกับ provinceId ถูกต้อง และข้อมูลอยู่ครบหลังปิดเปิดแอปใหม่ (อ้างอิง US-4) — dependency: T3, T12
- [ ] T15: ถ้าเป็น entry แรกของจังหวัด เปลี่ยนสถานะเป็น unlocked ทันทีหลังบันทึก (อ้างอิง US-4) — dependency: T14, T5

### US-3: อนิเมชันปลดล็อก 3 มิติ
- [ ] T16: อนิเมชัน spring/bounce เมื่อจังหวัดเปลี่ยนจาก flat/เทา เป็น elevated/เขียว ตอนกลับสู่หน้าแผนที่หลังบันทึก entry แรก **[รอ design-spec]** (อ้างอิง US-3) — dependency: T9, T15
- [ ] T17: Track สถานะ "เพิ่งเปลี่ยน" เพื่อไม่เล่นอนิเมชันซ้ำกับจังหวัดที่ unlocked อยู่ก่อนแล้ว (อ้างอิง US-3) — dependency: T16

### US-5: รายการบันทึกของจังหวัด
- [ ] T18: หน้า ProvinceDetail แสดงชื่อจังหวัดภาษาไทย (nameTh) เป็นหัวข้อหลัก (อ้างอิง US-5) — dependency: T11
- [ ] T19: List entry เรียง reverse-chronological ตามฟิลด์ date (อ้างอิง US-5) — dependency: T14
- [ ] T20: แต่ละ entry ใน list แสดง title, date, thumbnail รูปแรก (ถ้ามี) **[รอ design-spec]** (อ้างอิง US-5) — dependency: T19
- [ ] T21: Empty state สำหรับจังหวัดที่ไม่มี entry พร้อมปุ่ม/ทางเข้าไปเพิ่ม entry แรก (ครอบคลุม AC ของ US-2 ที่ว่าจังหวัด locked ต้องไม่ dead-end ด้วย) **[รอ design-spec]** (อ้างอิง US-5, US-2) — dependency: T18, T12

### Foundation รอบ Landmark & Supabase Integration (ไม่ต้องรอ design-spec — data layer, sync logic, auth logic)
- [ ] T33: ยืนยันเวอร์ชัน dependency ที่ต้องใช้ (`@supabase/supabase-js`, `@react-native-async-storage/async-storage`, `react-native-url-polyfill` — ตรวจสอบแล้วทั้ง 3 ตัวมีอยู่ใน package.json แล้ว **ไม่ต้องติดตั้งเพิ่ม**) แล้วเขียน Supabase client init module (env-based config `SUPABASE_URL`/`SUPABASE_ANON_KEY`) โดยใช้ mock/local client แทนการเชื่อมต่อ production project จริง ตามสมมติฐานในรอบนี้ (อ้างอิง US-12, US-13) — dependency: ไม่มี (ดูคำตัดสิน PM ข้อ 8)
- [ ] T34: ขยาย SQLite schema เดิม (จาก T3): เพิ่มตาราง landmark check-in (landmarkId, provinceId, visited, updatedAt) และเพิ่มคอลัมน์ sync metadata บนตาราง entries เดิม (updatedAt, syncedAt/pendingFlag, cloudId) — dependency: T3
- [ ] T35: สร้าง landmark seed dataset `src/data/thailand-landmarks.ts` ตามคำตัดสิน PM ข้อ 5 — จังหวัดนำร่อง (ข้อเสนอ: กรุงเทพมหานคร, เชียงใหม่, เชียงราย, พระนครศรีอยุธยา, สุโขทัย, ชลบุรี, กระบี่, ภูเก็ต) จังหวัดละ 3–5 landmark พร้อมชื่อ ส่วนอีก 68 จังหวัดที่เหลือใส่ array ว่าง/TBD (อ้างอิง US-8) — dependency: T2
- [ ] T36: Derived-state utilities สำหรับ landmark: จำนวนเช็คอินแล้ว/ทั้งหมดต่อจังหวัด และ boolean "Province Master" (เช็คอินครบ AND มี landmark อย่างน้อย 1 แห่ง) — dependency: T34, T35
- [ ] T37: Anonymous Auth session bootstrap — สร้าง session อัตโนมัติเมื่อเปิดแอปครั้งแรก/ไม่มี session ผูกอยู่ ผ่าน mock/local Supabase Auth client, เก็บ session ไว้ใน local storage (อ้างอิง US-11) — dependency: T33
- [ ] T38: Email-link service logic — ผูก email+password เข้ากับ anonymous session เดิม, คืน error ที่สื่อความหมายเมื่อ link ล้มเหลว (email ซ้ำ/รหัสผ่านไม่ผ่านเงื่อนไข) โดย session เดิมยังใช้งานต่อได้ (อ้างอิง US-11) — dependency: T37
- [ ] T39: Sync engine หลัก — ตรวจจับ pending queue (entries/check-ins ที่ updatedAt ใหม่กว่า syncedAt ล่าสุดหรือไม่เคย sync) และ trigger sync ขึ้น Supabase (mock client) อัตโนมัติเมื่อมีอินเทอร์เน็ต โดยไม่บล็อก UI (อ้างอิง US-12) — dependency: T34, T33
  - หมายเหตุ (คำตัดสิน PM ข้อ 9): เพิ่ม logic นับจำนวนครั้งที่ sync ล้มเหลวติดต่อกันต่อ record เพื่อ trigger sync-state ที่ 3 "retry-issue" (ดู T51) — เป็น scope เพิ่มเล็กน้อยจากที่ตั้งไว้เดิม (เดิมมีแค่ pending/synced), ยอมรับตาม design-spec
- [ ] T40: Conflict resolution แบบ last-write-wins — เทียบ `updated_at` ทั้ง record, ฝั่งแพ้ถูกเขียนทับด้วยฝั่งชนะในทั้งสองเครื่องหลัง sync รอบถัดไป (อ้างอิง US-12) — dependency: T39
  - หมายเหตุ (คำตัดสิน PM ข้อ 7): field-level merge อยู่นอกขอบเขตของรอบนี้โดยชัดเจน ใช้ whole-record last-write-wins เท่านั้น — ห้าม QA ตีความว่าเป็น missed AC
- [ ] T41: Photo upload sync logic — คิวรูปที่ยังไม่เคยอัปโหลด, อัปโหลดขึ้น Supabase Storage bucket `trip-photos` (mock client) แล้วบันทึก URL คืนเข้า entry, retry เมื่อเน็ตหลุดโดยไม่สร้างรูปซ้ำ (อ้างอิง US-13) — dependency: T39
- [ ] T42: Migration logic — เมื่อสร้าง session ครั้งแรก ตรวจหา entries เดิม (v1, ไม่มี sync metadata) แล้ว mark เป็น pending sync ทั้งหมดโดยอัตโนมัติ เข้าคิวเข้า flow ของ T39 ทันที ทำงานแบบไม่บล็อกแอป และ resume ได้ถ้าล้มเหลวบางส่วนโดยไม่ทำข้อมูลเดิมเสียหาย (อ้างอิง US-15) — dependency: T34, T37, T39

### US-8: รายการ Landmark และเช็คอิน
- [ ] T43: หน้า ProvinceDetailScreen แสดงรายการ Landmark ของจังหวัดจาก `thailand-landmarks.ts` พร้อมปุ่ม/สวิตช์ check-in ที่ persist ลง local storage ทันที **[รอ design-spec]** (อ้างอิง US-8) — dependency: T35, T36
- [ ] T44: Progress indicator "เช็คอินแล้ว X/Y แห่ง" อัปเดตทันทีเมื่อ toggle **[รอ design-spec]** (อ้างอิง US-8) — dependency: T36, T43
- [ ] T45: Empty state "ยังไม่มีข้อมูลสถานที่แนะนำ" สำหรับจังหวัดที่ยังไม่มีข้อมูล landmark **[รอ design-spec]** (อ้างอิง US-8) — dependency: T43

### US-9: ตราสัญลักษณ์ Province Master
- [ ] T46: แสดงตราสัญลักษณ์ "Province Master ⭐" บน ProvinceDetailScreen เมื่อเช็คอินครบ, คำนวณสดจากสถานะปัจจุบัน (หายทันทีถ้า toggle กลับจนไม่ครบ), ไม่แสดงถ้าจังหวัดไม่มี landmark เลย **[รอ design-spec]** (อ้างอิง US-9) — dependency: T36, T43

### US-10: เลือก Landmark ตอนเพิ่มบันทึก (auto check-in)
- [ ] T47: AddEntryScreen เพิ่ม field เลือก Landmark (optional) แสดงเฉพาะของจังหวัดที่กำลังเพิ่ม entry อยู่, ซ่อน/disabled พร้อมข้อความถ้าไม่มีข้อมูล **[รอ design-spec]** (อ้างอิง US-10) — dependency: T35, T12
- [ ] T48: เมื่อบันทึก entry ที่เลือก Landmark ไว้ อัปเดตสถานะ check-in ของ landmark นั้นเป็น visited อัตโนมัติ โดยไม่กระทบ flow เดิมของ US-4 เมื่อไม่เลือก landmark ใดๆ (อ้างอิง US-10) — dependency: T47, T34

### US-11: Anonymous Auth + ผูก Email ภายหลัง
- [ ] T49: หน้า/ปุ่ม "ผูกกับอีเมล" ในหน้าตั้งค่า/โปรไฟล์ เชื่อมกับ T38, ข้อมูลเดิม (entries/check-ins) ยังอยู่ครบหลัง link สำเร็จ **[รอ design-spec]** (อ้างอิง US-11) — dependency: T38
- [ ] T50: คำเตือนความเสี่ยงข้อมูลหาย (data-loss warning) ตาม constraint ในคำตัดสิน PM ข้อ 6 ด้านล่าง — ต้องมีทั้ง one-time prompt ตอนสร้าง anonymous session และจุดที่มองเห็นได้เสมอในหน้าตั้งค่า/โปรไฟล์ตราบใดที่ยังไม่ผูก email **[รอ design-spec]** (อ้างอิง US-11) — dependency: T37
  - หมายเหตุ (คำตัดสิน PM ข้อ 10): ยืนยันรับดีไซน์ non-dismissible banner ตามที่ UIUX เลือก (ไม่มีปุ่มปิด แสดงถาวรในหน้าตั้งค่าจนกว่าจะผูก email) — อยู่ในกรอบดุลยพินิจที่ PM ให้ไว้แล้ว ไม่ต้องมี logic re-surface/นับวันเพิ่ม

### US-12: ซิงค์ entries และ landmark check-in แบบ Offline-First
- [ ] T51: Sync status indicator (badge/ไอคอน 3 สถานะ: "รอซิงก์" / "ซิงก์แล้ว" / "retry-issue" สำหรับ sync ล้มเหลวต่อเนื่องหลายครั้ง) บน entry list/detail **[รอ design-spec]** (อ้างอิง US-12) — dependency: T39, T19, T20
  - หมายเหตุ (คำตัดสิน PM ข้อ 9): เพิ่ม state ที่ 3 "retry-issue" ตามที่ design-spec เสนอ (เกินขั้นต่ำ 2 สถานะของ AC เดิมเล็กน้อย) — ยอมรับเพราะยังเป็น in-app indicator ล้วนๆ ไม่ใช่ push notification ตาม Out of Scope และช่วยไม่ให้ผู้ใช้เข้าใจผิดว่าข้อมูลหายเงียบๆ เมื่อ sync ล้มเหลวนาน

### US-13: อัปโหลดรูปขึ้น Supabase Storage
- (ครอบคลุมด้วย T41 ใน layer ข้อมูล — ไม่มี UI เพิ่มเติมเฉพาะ นอกจาก sync status indicator ที่ T51 ครอบคลุมร่วมกันแล้ว)

### US-14: Visual Feedback "Province Master" บนแผนที่ 3 มิติ
- [ ] T52: จังหวัดที่มีสถานะ Province Master แสดงเอฟเฟกต์พิเศษ (เช่น ขอบทอง/มงกุฎ) แตกต่างจาก unlocked ปกติบน HomeScreen 3D map, อัปเดตสดโดยไม่ต้องปิดเปิดแอปใหม่เมื่อสถานะเปลี่ยนระหว่างอยู่ในแอป **[รอ design-spec]** (อ้างอิง US-14) — dependency: T36, T9
- [ ] T53: อัปเดต legend ใต้แผนที่ (จาก T23 เดิม) ให้มีคำอธิบายสถานะ Province Master เพิ่มเติม **[รอ design-spec]** (อ้างอิง US-14) — dependency: T52, T23

### US-15: Migrate ข้อมูล Local-Only เดิม (v1) ขึ้น Cloud
- (ครอบคลุมด้วย T42 ใน Foundation ด้านบน — เป็น P0 logic task ตามคำตัดสิน PM ข้อ 4/รายการ decision ด้านล่าง ไม่มี UI เฉพาะเพิ่มเติมเพราะ AC ระบุว่าต้องทำงานอัตโนมัติแบบไม่บล็อกและไม่ต้องให้ผู้ใช้กดอะไร)

### US-16: สคริปต์ดึงข้อมูล Top Landmark จาก OSM Overpass API (one-time/build-time)
- [ ] T60: เขียน Node/CLI extraction script แยกต่างหาก (เช่น `scripts/extract-overpass-landmarks.ts`) — เครื่องมือ dev/build-time เท่านั้น ห้าม import จากโค้ด runtime ของแอปและห้าม bundle เข้า production app bundle เด็ดขาด สคริปต์ query Overpass API ต่อจังหวัดจากทั้ง 76 จังหวัดใน `thailand-provinces.ts` (อ้างอิง centroid + bounding box/area ของแต่ละจังหวัดตามสมมติฐานที่ระบุ) โดย filter เฉพาะ tags: `tourism=attraction`, `historic=monument|temple`, `amenity=place_of_worship`, `leisure=park`, จัดลำดับความสำคัญของ tag แล้วคัดเลือกสูงสุด 3–5 แห่งต่อจังหวัด, ตัดรายการที่ไม่มีพิกัด lat/lng ที่ถูกต้องออก (ไม่บันทึกพิกัดว่าง/NaN), หน่วงเวลาระหว่าง request แต่ละจังหวัด + retry อย่างน้อย 1 ครั้งเมื่อ fail/timeout/rate-limit โดยความล้มเหลวของจังหวัดหนึ่งต้องไม่ทำให้ผลลัพธ์จังหวัดอื่นที่สำเร็จแล้วเสียหาย, เขียนผลลัพธ์ออกเป็นไฟล์ data กลาง (เช่น JSON) ที่ยังไม่ทับ `thailand-landmarks.ts` โดยตรง (ให้ T64 เป็นคนรวมภายหลัง), เมื่อรันจบสรุปผล + log แยกรายชื่อจังหวัดที่ได้ไม่ครบ 3 แห่งหรือ 0 แห่งไว้ต่างหาก (อ้างอิง US-16) — dependency: T2, T35

### US-17: รวมข้อมูล Landmark (พร้อมพิกัด) เข้าแอปแบบ Offline และออกแบบตาราง Supabase รองรับ
- [ ] T62: ขยาย `Landmark` type เพิ่มฟิลด์ `lat`/`lng` เป็น **optional** (ชื่อ field ให้ตรงตามคำที่ AC ของ US-16/US-17 ใช้คือ `lat`/`lng` ไม่ใช่ `latitude`/`longitude` เต็มคำ) เพื่อ backward-compat กับ landmark ที่อาจยังไม่มีพิกัด — แก้ที่ interface ซึ่งประกาศอยู่ใน `src/data/thailand-landmarks.ts` เอง (ไม่ใช่ `src/types/landmark.ts` ที่มีแค่ `LandmarkCheckin`) เพื่อลด diff ไม่ต้องย้ายไฟล์ (อ้างอิง US-16 AC4, US-17) — dependency: T35 (ดูคำตัดสิน PM ข้อ 14)
- [ ] T63: Backfill พิกัด lat/lng ให้ landmark ทั้งหมดของ 8 จังหวัดนำร่องเดิม (T35) ด้วยการ lookup แยกต่างหาก (เช่น ยิง Overpass/Nominatim หาแต่ละชื่อ landmark ทีละรายการด้วยมือ/สคริปต์ช่วย) — **ห้ามเปลี่ยนหรือแทนที่ id เดิมของ landmark เหล่านี้เด็ดขาด** เพราะ `CheckinContext` (T36) ผูกสถานะ check-in ของผู้ใช้ด้วย `landmarkId` string เดิมโดยตรง (อ้างอิง US-17 AC5) — dependency: T62, T35
- [ ] T64: Merge ผลลัพธ์จาก T60 (ใช้ผลจาก T61 ถ้ารันจริงสำเร็จ ไม่งั้นใช้ fallback data ตาม T61) เข้า `thailand-landmarks.ts` **เฉพาะ 68 จังหวัดที่เหลือซึ่งปัจจุบันเป็น array ว่าง** — เติมเสริมเท่านั้น ห้ามแทนที่/เขียนทับ landmark ของ 8 จังหวัดนำร่องเดิม ตามคำตัดสิน PM ข้อ 12 (อ้างอิง US-17 AC1,5) — dependency: T60, T62, T63
- [ ] T65: Regression check ว่า T43/T44/T46/T47 (US-8/US-9/US-10 เดิม) ยังทำงานถูกต้องกับ dataset ที่ขยายแล้ว (จำนวน landmark ต่อจังหวัดเพิ่มขึ้น และมี lat/lng เพิ่มเติม แต่ logic เดิมอ่านแค่ id/provinceId/nameTh ต้องไม่พัง) (อ้างอิง US-17 AC1,2,4) — dependency: T64
- [ ] T66: ออกแบบตาราง Supabase `landmarks` (schema: `id`, `province_id`, `name_th`, `lat`, `lng`) พร้อม seed/sync logic นำข้อมูลจาก local dataset ขึ้น Supabase เมื่อมีการเชื่อมต่อ ทดสอบผ่าน mocked/local Supabase client เท่านั้น (ตาม pattern เดียวกับ T39/T41) (อ้างอิง US-17 AC3) — dependency: T64, T39, T33

### US-18: เห็นตำแหน่ง Landmark บนแผนที่ระดับจังหวัด
- [ ] T67: ProvinceDetailScreen แสดงตำแหน่ง (lat/lng) ของ Landmark แต่ละแห่งที่มีพิกัดครบ เป็นจุด/หมุดบนองค์ประกอบแผนที่ของหน้านั้น, Landmark ที่ไม่มีพิกัดครบถูกกรองออกจากแผนที่แต่ยังปรากฏใน list ปกติของ T43 ตามเดิม (ไม่ error, ไม่ทำให้ landmark อื่นหาย), การแตะ/เลือกจุด (ถ้าออกแบบให้กดได้) ต้องอ้างอิง landmark id เดียวกับรายการใน list เสมอ, ทำงาน 100% offline จาก local dataset/cache เท่านั้น ไม่เรียก Overpass หรือบริการแผนที่ภายนอกใดๆ **[รอ design-spec]** (อ้างอิง US-18) — dependency: T64, T63, T43

### US-19: ค้นหาสถานที่ผ่าน OSM Nominatim ตอนเพิ่มบันทึกทริป
- [ ] T68: เขียน Nominatim client module — ยิง request ไปยัง `https://nominatim.openstreetmap.org/search?format=json&countrycodes=th&q=...` พร้อมแนบ custom `User-Agent` header คงที่ (ค่าที่ทีม dev กำหนดเอง ไม่ใช่ default ของ HTTP client) ตาม OSM Usage Policy, จัดการ error/timeout ที่ระดับ client แยกจาก UI layer (อ้างอิง US-19 AC2, US-20 AC1) — dependency: T1
- [ ] T69: ขยาย entries schema (ต่อจาก T3/T34) เพิ่มคอลัมน์ optional `placeLat`/`placeLng` สำหรับเก็บ metadata พิกัดของสถานที่จาก Nominatim search แนบกับ entry — แยกขาดจากตาราง landmark check-in เดิม (T34) โดยสิ้นเชิง ไม่ปนกัน (อ้างอิง US-19 AC3) — dependency: T3, T34
- [ ] T70: AddEntryScreen เพิ่มช่องค้นหาสถานที่แบบ optional เพิ่มเติมจาก field เลือก Landmark เดิม (T47) สำหรับค้นหาสถานที่ที่ไม่อยู่ใน Top Landmarks ของจังหวัดนั้น, เรียกใช้ T68 เมื่อพิมพ์คำค้นและมีเน็ต แล้วแสดงรายการผลลัพธ์เป็น list ให้เลือก, ไม่บังคับใช้งานและไม่กระทบ flow เดิมของ US-4/US-10 ถ้าไม่ใช้ **[รอ design-spec]** (อ้างอิง US-19 AC1,2) — dependency: T68, T47, T12
- [ ] T71: เมื่อผู้ใช้เลือกผลลัพธ์รายการหนึ่งจาก T70 ระบบ prefill ค่าไปยังฟิลด์ "ชื่อสถานที่" (title) ของฟอร์มเดิมโดยอัตโนมัติ (ผู้ใช้ยังแก้ไขค่าที่ prefill มาได้ก่อนบันทึกจริง) พร้อมเก็บพิกัด lat/lng ของผลลัพธ์นั้นแนบไปกับ entry ผ่านคอลัมน์ `placeLat`/`placeLng` จาก T69 — **การเลือกผลลัพธ์นี้ต้องไม่ทำให้เกิดการสร้าง Landmark ใหม่ในตาราง/seed dataset และต้องไม่ trigger auto check-in ตาม US-10 เด็ดขาด** แยกกลไกออกจาก dropdown เลือก Landmark ที่ curate ไว้แล้ว (T47/T48) อย่างสมบูรณ์ (อ้างอิง US-19 AC3,4) — dependency: T70, T69 (ดูคำตัดสิน PM ข้อ 11)

### US-20: จัดการ Rate Limit, Error และ Offline State ของการค้นหาผ่าน Nominatim
- [ ] T72: Debounce การพิมพ์คำค้นใน T70 (ยิง request จริงหลังผู้ใช้หยุดพิมพ์ระยะเวลาหนึ่ง) และ throttle guard ที่ระดับ client module T68 ไม่ให้ยิง request ถี่กว่า ~1 ครั้งต่อวินาที แม้ผู้ใช้พิมพ์เปลี่ยนคำค้นต่อเนื่องเร็วๆ (อ้างอิง US-20 AC2) — dependency: T68, T70
- [ ] T73: แสดงสถานะเมื่อค้นหาไม่ได้ (ไม่มีเน็ต/request ล้มเหลว/timeout) ด้วยข้อความสื่อความหมายชัดเจน (เช่น "ค้นหาไม่ได้ในขณะนี้ ลองใหม่อีกครั้ง") แทนการค้าง loading ตลอดไปหรือ error แบบ raw, แสดง empty state ("ไม่พบสถานที่ที่ค้นหา") เมื่อไม่พบผลลัพธ์เลย — ตรวจสอบว่า "มีเน็ตหรือไม่" ด้วยวิธี opportunistic (ยิง request แล้ว catch error) แบบเดียวกับ `src/sync/SyncContext.tsx` ไม่ใช้ event-based network detection (ไม่มี `@react-native-community/netinfo` ติดตั้ง) **[รอ design-spec]** (อ้างอิง US-20 AC3,4,5) — dependency: T70, T68
- [ ] T74: Isolate failure boundary ของระบบค้นหา Nominatim ทั้งหมด (T68–T73) ไม่ให้กระทบ flow กรอก/บันทึก entry ปกติของ US-4 เลย — ผู้ใช้ยังกรอก title เองแล้วบันทึกได้ตามปกติแม้ระบบค้นหาใช้งานไม่ได้ชั่วคราวทั้งระบบ (อ้างอิง US-20 AC6) — dependency: T70, T12

### รอบ 5: Landmark Cards & Media Integration (US-21–US-24) — งานนี้ implement มาแล้วก่อนเข้า pipeline (retroactive)
> หมายเหตุ: โค้ดของรอบนี้ถูกเขียนไปแล้วนอก pipeline ก่อนที่ BA/PM จะเห็นโจทย์ — task ด้านล่างจึงเป็นการ map สิ่งที่ implement แล้วเข้ากับ US-21–US-24 ของ requirements.md เพื่อให้ Tester/QA ตรวจสอบย้อนกลับ ไม่ใช่ breakdown ก่อนเริ่มงานตามปกติ ทุก task ในหมวดนี้ทำเครื่องหมาย [x] ตามที่รายงานว่า implement แล้ว — **สถานะ [x] นี้ยังไม่ผ่านการยืนยันจาก Tester/QA อิสระ** ห้าม programmer/QA รอบถัดไปเชื่อว่า "เสร็จแล้วแน่นอน" จนกว่าจะเห็น docs/test-report.md และ docs/qa-result.md ของรอบนี้
- [x] T76: แปลงรูปเป็น JPEG ทันทีที่เลือกจากแกลเลอรีด้วย `expo-image-manipulator` (ไฟล์: `src/components/PhotoPicker.tsx`) พร้อม fallback ใช้ URI เดิมถ้าแปลงล้มเหลว (อ้างอิง US-21) — dependency: T13
- [x] T77: บังคับ `contentType: 'image/jpeg'` ตอนอัปโหลดรูปขึ้น Supabase Storage bucket `trip-photos` (ไฟล์: `src/sync/photoUpload.ts`) (อ้างอิง US-21) — dependency: T41, T76
- [x] T78: สร้าง `src/services/wikipediaService.ts` — `fetchAttractionsForProvince()` ดึงสถานที่ผ่าน MediaWiki category API ของ th.wikipedia.org (4 หมวดหมู่จังหวัด), fallback ดึงรูปจาก Wikimedia Commons เมื่อไม่มี thumbnail, จำแนกหมวดหมู่อัตโนมัติ 5 หมวด (`detectCategory`), in-memory cache ต่อจังหวัด (อ้างอิง US-22) — dependency: T35
- [x] T79: ขยาย `Landmark` interface ใน `src/data/thailand-landmarks.ts` เพิ่ม `imageUrl?`, `description?`, `category?` (optional, backward-compat กับ Landmark เดิมที่ไม่มีฟิลด์เหล่านี้) — **หมายเหตุจุดที่ต้องเช็ค**: ตรวจสอบด้วย grep แล้วพบว่าฟิลด์นี้ถูกเพิ่มใน `thailand-landmarks.ts` จริง (ไม่ใช่ `src/types/landmark.ts` ตามที่รายงานสรุปไว้ — `src/types/landmark.ts` มีแค่ `LandmarkCheckin` เหมือนเดิม ไม่ได้ถูกแก้ในรอบนี้ ไม่ตรงกับที่รายงาน) — ให้ Tester ยืนยันซ้ำอีกครั้งว่าไม่มีจุดอื่นที่ type ไม่ตรงกัน (อ้างอิง US-22) — dependency: T62
- [x] T80: สร้าง `src/components/LandmarkCard.tsx` — การ์ดรูปภาพ+ป้ายหมวดหมู่+คำอธิบาย+ปุ่มเช็คอิน (`accessibilityRole="switch"`) ผูกกับ `CheckinContext` เดิม (อ้างอิง US-22) — dependency: T43, T78, T79
- [x] T81: อัปเดต `src/components/LandmarkList.tsx` — เรียก `fetchAttractionsForProvince` มาผสานกับ local seed data เดิม, render ด้วย `LandmarkCard`, จัดการ error/offline ของ Wikipedia fetch ให้ fallback เหลือแค่ local data โดยไม่ error (อ้างอิง US-22) — dependency: T78, T80
  - **หมายเหตุ (แก้ไขจาก US-25 bug fix, ดู T87–T89 ด้านล่าง)**: พฤติกรรม "จัดการ error/offline ของ Wikipedia fetch ให้ fallback เหลือแค่ local data โดยไม่ error" ตามที่เขียนไว้เดิมนี้ **ยังคงใช้ได้บางส่วนเท่านั้น** — เดิมโค้ด catch แล้วเงียบสนิททุกเคสรวมถึงเคส fetch ล้มเหลวจริง ซึ่งขัดกับ US-25 ที่ต้องแยกแสดง error state ที่มองเห็นได้ ไม่ใช่เงียบสนิทเสมอไป T87–T89 คือการแก้ไข้พฤติกรรมนี้ให้ตรงตาม US-25 โดยยังคง "local data ไม่หาย" ไว้ตามเดิม (AC4 ของ US-25)
- [x] T82: In-province search bar ใน `LandmarkList.tsx` — กรอง Landmark ของจังหวัดนั้นแบบ real-time ตามชื่อ (อ้างอิง US-23) — dependency: T81
- [x] T83: Category filter chips ใน `LandmarkList.tsx` — กรองตามหมวดหมู่ 5 หมวด + "ทั้งหมด" ใช้ร่วมกับ search ได้ (AND condition), ไม่กระทบ progress indicator/LandmarkMap ที่ยังต้องอ่านจากรายการเต็ม (อ้างอิง US-23) — dependency: T82, T44, T67
- [x] T84: สร้าง `src/components/GlobalSearchBar.tsx` — ค้นหาจังหวัด (nameTh/nameEn) + Landmark จาก local dataset (`thailand-landmarks.ts`) แบบ offline, dropdown ผลลัพธ์พร้อมไอคอนแยกประเภท, แตะแล้ว navigate ไป ProvinceDetailScreen (อ้างอิง US-24) — dependency: T2, T35
- [x] T85: Wire `GlobalSearchBar` เข้า `HomeScreen.tsx` เหนือแผนที่ 3D (อ้างอิง US-24) — dependency: T84, T8

**สิ่งที่ PM พบระหว่างตรวจสอบ ต้องให้ Tester ตรวจเพิ่ม (ไม่ใช่ task ใหม่ แต่เป็นจุดเสี่ยงที่ report ของผู้ implement ไม่ได้พูดถึง):**
- พบไฟล์ `src/components/LandmarkCardGrid.tsx` อยู่ในโปรเจกต์ (untracked) แต่ **ไม่มีที่ใดใน source เรียกใช้เลย** (grep แล้วไม่เจอ import จากไฟล์อื่น) — ดูเหมือน dead code/ไฟล์ค้างจากการ implement (อาจเป็น component ที่เขียนแล้วไม่ได้ใช้จริง หรือของเก่าที่ถูกแทนด้วย `LandmarkCard.tsx`) ต้องให้ Tester ยืนยันว่าไม่ได้ถูกอ้างถึงจริง แล้วเสนอ programmer ลบทิ้งในรอบแก้บั๊กถัดไปถ้ายืนยันว่าไม่ใช้

### รอบ 6: Bug Fix — US-25 แยก "จังหวัดยังไม่มีข้อมูล Landmark" ออกจาก "ดึงข้อมูลจาก Wikipedia ล้มเหลว" พร้อมทางลองใหม่
> บริบท: ปัจจุบันตรวจโค้ดจริงแล้วพบว่า `src/services/wikipediaService.ts` มี `catch { }` ที่เงียบสนิททั้ง 3 จุด — ใน `fetchCommonsPhoto` (บรรทัดประมาณ 43), ใน loop ต่อหมวดหมู่ของ `fetchAttractionsForProvince` (บรรทัดประมาณ 117, "Continue to next category"), และใน `searchAttractionsGlobal` (บรรทัดประมาณ 181) — ทำให้ network error/timeout/HTTP error ที่เกิดขึ้นจริงกับกรณี "query สำเร็จแต่ไม่มีบทความ" คืนผลลัพธ์แบบแยกไม่ออกจากกัน (ทั้งคู่จบที่ array ว่างเหมือนกัน) ฝั่ง `src/components/LandmarkList.tsx` เงื่อนไข `landmarks.length === 0 && !isFetchingWiki` (บรรทัดประมาณ 141) และ `catch { // Fallback: keep existing landmarks }` ในตัว effect ดึงข้อมูล (บรรทัดประมาณ 97) ก็ครอบคลุมสองเคสนี้ปนกันเช่นกัน เป็น root cause ตรงตาม bug ที่ US-25 อธิบาย — **หมายเหตุสำคัญ**: มีงาน UI redesign คู่ขนาน (Advanced UI/UX Upgrade) แก้ `LandmarkList.tsx` ไปแล้วบางส่วน (Bento Grid, ShimmerBlock, PressableScale) แต่ยังไม่แตะ error-handling logic เลย ผู้รับ task ต้องอ่านโค้ดปัจจุบันจริงก่อนแก้ ไม่ใช้เลขบรรทัด/โครงสร้างที่อ้างในนี้เป็นค่าตายตัว
- [x] T87: แก้ `src/services/wikipediaService.ts` ให้แยกแยะ "ทุก category query สำเร็จแต่ไม่มีบทความจริง" (คง behavior เดิม คือ resolve เป็น array ว่าง) ออกจาก "การดึงข้อมูลล้มเหลวจริง" (network error/timeout/HTTP error/parse error) — เคสหลังต้อง throw/reject ออกไปให้ caller รู้ แทนที่จะ catch แล้วเงียบสนิทเหมือนปัจจุบัน อนุญาตให้คง per-category catch-and-continue ไว้ได้ถ้ามีอย่างน้อย 1 category สำเร็จจริง (ไม่ throw) แต่ถ้าทุก category ล้มเหลวด้วย error จริงล้วนๆ (ไม่ใช่แค่ไม่มีบทความ) ฟังก์ชันต้อง throw รวมออกไป — ส่วน `fetchCommonsPhoto` (fetch รูปเสริม) ต้องไม่ทำให้ทั้งฟังก์ชันหลัก throw ผิดเคส เพราะความล้มเหลวของรูปเสริมเพียงอย่างเดียวไม่ถือเป็น "ดึงข้อมูลสถานที่ล้มเหลว" ตาม AC ของ US-25 (อ้างอิง US-25 AC1,2,5) — dependency: T78
- [x] T88: แก้ `src/components/LandmarkList.tsx` เพิ่ม error state ใหม่ (เช่น state แยก เช่น `fetchError`) ที่แสดงผลแยกจาก `EmptyStateLandmarks` เดิมอย่างชัดเจน — เมื่อ effect ที่เรียก `fetchAttractionsForProvince` (จาก T87) throw จริง ให้ set error state และแสดงข้อความสื่อความหมายว่าดึงข้อมูลไม่สำเร็จ (เช่น "โหลดข้อมูลสถานที่ไม่สำเร็จ"), ต้องไม่ใช้ข้อความ/component เดียวกับ empty state เดิม, เมื่อ resolve สำเร็จเป็น array ว่างยังคงแสดง `EmptyStateLandmarks` เดิมทุกประการเหมือนก่อนแก้ — สำคัญ: ต้องตรวจสอบให้ landmark จาก local seed dataset (ถ้าจังหวัดนั้นมีอยู่แล้วจาก T35/T64) ยังคงแสดงผลได้ปกติครบถ้วนเสมอแม้ Wikipedia fetch ล้มเหลว โดย error state แสดงแยกเป็นส่วนเสริมของหน้าเท่านั้น ไม่ทับ/ซ่อน/แทนที่ local landmark ที่มีอยู่แล้ว (regression guard สำหรับ AC เดิมของ US-22) (อ้างอิง US-25 AC1,2,4) — dependency: T87, T81
- [x] T89: เพิ่มทางลองใหม่ในหน้าจอเดียวกันสำหรับ error state ของ T88 — เมื่อผู้ใช้สั่งลองใหม่ ระบบต้องเรียก `fetchAttractionsForProvince` ซ้ำทันทีโดยไม่ต้องออกจากหน้าจังหวัดนั้นแล้วกลับเข้ามาใหม่ (ไม่ระบุรูปแบบ UI ที่แน่นอนตาม AC), ระหว่างลองใหม่แสดง loading state ที่เหมาะสม (เช่น reuse `ShimmerBlock` เดิมที่มีอยู่แล้วในไฟล์นี้), ถ้ารอบใหม่สำเร็จต้องเคลียร์ error state เดิมออกทั้งหมดและแสดงผลลัพธ์ Landmark ที่ดึงมาได้ตามปกติ (เหมือนกรณี fetch สำเร็จตั้งแต่แรกของ US-22), ถ้ารอบใหม่ล้มเหลวอีกให้ error state เดิมยังคงอยู่ (ไม่ค้าง loading ตลอดไป) (อ้างอิง US-25 AC3,6) — dependency: T88
  - หมายเหตุ testability (อ้างอิง US-25 AC5): ต้องเขียน/ปรับ test ให้ mock `fetchAttractionsForProvince` แยกสองเคสได้ชัดเจน — mock reject/throw ต้องเห็น error state (ไม่ใช่ empty state, ไม่ค้าง loading ตลอดไป) และ mock resolve เป็น array ว่างต้องเห็น empty state เดิม (ไม่ใช่ error state) เป็น regression guard กันไม่ให้ T87–T89 พังกลับไปเป็นพฤติกรรมเดิมที่ปนกันอีก

### รอบ 7: Bottom Tab Navigation & ข่าวท่องเที่ยว RSS (US-28 ถึง US-32)

#### Verification & Dependencies (ต้องทำก่อนสายอื่นทั้งหมด — ไม่ต้องรอ design-spec)
- [ ] T90: **[ทำก่อนสุด]** Verify endpoint RSS จริง `https://www.tatnews.org/feed/` ว่า active และคืนค่าเป็น valid RSS/Atom XML จริงหรือไม่ ณ เวลา implement — ถ้าใช้ไม่ได้ ให้ลองตามลำดับ fallback ที่กำหนดไว้ในคำตัดสิน PM ข้อ 19 แล้ว log ผลสรุปว่าสุดท้ายใช้แหล่งใดไว้ใน `docs/dev-notes.md` (อ้างอิง US-29 AC1) — dependency: ไม่มี
- [ ] T91: ติดตั้ง dependency ที่ยังไม่มีในโปรเจกต์ (ตรวจสอบ `package.json` แล้วยืนยันว่าไม่มีจริง — ดูคำตัดสิน PM ข้อ 20): `@react-navigation/bottom-tabs` (สำหรับ US-28) และ `expo-web-browser` (สำหรับ US-31) พร้อมตรวจสอบว่าชุดไอคอน (เช่น `@expo/vector-icons` ที่มักมากับ Expo SDK อยู่แล้วโดยไม่ต้องเพิ่มใน `package.json`) ใช้งานได้จริงในโปรเจกต์นี้หรือไม่ ถ้าไม่ได้ให้ fallback เป็น text label ภาษาไทยไปก่อนจนกว่า UIUX จะกำหนดไอคอนจริงใน T94 — dependency: ไม่มี

#### US-28: Bottom Tab Navigation (สายนี้แยกจากสาย News โดยสิ้นเชิง — ความเสี่ยงคือ regression ทั้งแอป ไม่ใช่งานของใหม่)
- [ ] T92: ปรับโครงสร้าง `AppNavigator.tsx` จาก native stack เดี่ยวเป็น Bottom Tab Navigator 2 แท็บ — สร้าง nested stack navigator แยกสำหรับแท็บ "แผนที่" (MapStack: ครอบ Home/ProvinceDetail/AddEntry/Stats/Settings/Map2DValidation ตามเดิมทั้งหมดที่มีอยู่แล้วใน stack ปัจจุบัน) และแท็บ "ข่าว" (NewsStack: ครอบ NewsScreen ใหม่ + Stats/Settings) แล้วประกอบทั้งสอง stack เข้า Bottom Tab Navigator เป็น root — ใช้ text label ภาษาไทยขั้นต่ำก่อน ("แผนที่"/"ข่าว") ตาม AC ขั้นต่ำของ US-28 AC1 ส่วนไอคอนจริงรอ T94 (อ้างอิง US-28 AC1,2,3) — dependency: T91
- [ ] T93: ผูกปุ่มไอคอน Stats และ Settings ให้ปรากฏใน header ของทั้ง 2 แท็บ (ไม่ใช่ hardcode อ้างอิงเฉพาะ stack เดียวเหมือนก่อนหน้านี้) และ push เข้า stack ของแท็บปัจจุบันที่ผู้ใช้อยู่ให้ถูกต้อง (อ้างอิง US-28 AC4) — dependency: T92
- [ ] T94: ปรับไอคอน tab bar จริงตาม design-spec ของ UIUX (แทนที่ text label ชั่วคราวจาก T92) **[รอ design-spec]** (อ้างอิง US-28 AC1) — dependency: T92, T91
- [ ] T95: Regression ครบทุก flow เดิมที่แตะ root navigator หลังเปลี่ยนโครงสร้าง — ยืนยันว่า US-1/US-2/US-3/US-14/US-24/US-26 (แผนที่ 3D, global search bar, navigate ไป ProvinceDetail/AddEntry, ปุ่ม Stats/Settings) ยังทำงานถูกต้องทุกข้อไม่มี regression และเมื่อสลับไปแท็บ "ข่าว" แล้วกลับมาแท็บ "แผนที่" ต้องเห็นหน้าจอ/state เดิมที่ค้างอยู่ ไม่ reset กลับไปหน้าแรกของ stack (อ้างอิง US-28 AC2,5) — dependency: T92, T93
- [ ] T96: ยืนยัน route dev-only `Map2DValidation` ยังเข้าถึงได้ในโหมด dev เหมือนเดิมหลังย้ายเข้า nested MapStack ไม่ถูกลบ/เสียหายจากการเปลี่ยนโครงสร้าง navigation (อ้างอิง US-28 AC6) — dependency: T92

#### US-29 / US-30: News data & logic layer (สายอิสระจาก Navigation — ของใหม่ล้วนๆ)
- [ ] T97: เขียน RSS fetch module (เช่น `src/services/newsService.ts`) — ยิง request ไปยัง endpoint ที่ยืนยันจาก T90 พร้อม error handling ที่แยก network/timeout/HTTP error ออกจากผลลัพธ์สำเร็จ (อ้างอิง US-29 AC1, US-32 AC4) — dependency: T90
- [ ] T98: เขียน RSS/XML parser เอง (regex/string-based ตามคำตัดสิน PM ข้อ 21 — ไม่พึ่ง DOMParser/XML library ภายนอกเพราะ RN ไม่มี DOMParser ในตัว) แยก field ต่อรายการข่าว: `title`, `description`/summary (ตัด HTML tag ออกก่อนคืนค่า), `image` (ลำดับ: `<enclosure>` → `<media:content>` → รูปแรกที่ parse ได้จาก description HTML → placeholder ถ้าไม่มีเลย), `link`, `pubDate` — ต้องเขียนเป็น pure function ที่ inject ค่า mocked RSS XML string เข้าไป test ได้ตรงตาม AC ข้อสุดท้ายของ US-29 (อ้างอิง US-29 AC2,5) — dependency: T97
- [ ] T99: Post-processing pipeline ต่อจาก parser: (1) เรียงลำดับตาม `pubDate` ล่าสุดไปเก่าสุด, รายการที่ไม่มี `pubDate` ที่ parse ได้ถูกต้องให้จัดไว้ท้ายสุดเสมอไม่ทำให้การเรียงทั้งหมด error/พัง (2) deduplicate ตาม `link` เป็นหลัก (ถ้า `link` ว่างให้ fallback เทียบด้วย `title`) เก็บเฉพาะรายการแรกที่เจอ (อ้างอิง US-29 AC1, US-32 AC3,5) — dependency: T98
- [ ] T100: AsyncStorage cache layer — persist parsed news items + timestamp การดึงล่าสุดทุกครั้งที่ fetch สำเร็จ, TTL คงที่ 45 นาที (ดูคำตัดสิน PM ข้อ 23) ก่อน refetch อัตโนมัติเมื่อเปิดแท็บใหม่ (แสดง cache เดิมก่อนทันทีแบบ stale-while-revalidate แล้วสลับเป็นของใหม่เมื่อโหลดเสร็จโดยไม่บล็อกการแสดงผล), pull-to-refresh ต้องบังคับดึงใหม่ได้เสมอไม่สนใจ TTL, ถ้า fetch ล้มเหลวและมี cache เดิมให้ใช้ cache เดิมแสดงต่อ ถ้าไม่มี cache เลยให้ปล่อยให้ error state ของ T103 ทำงาน (อ้างอิง US-29 AC3,4, US-30 AC1,2,4) — dependency: T99

#### US-29 / US-30: NewsScreen UI (ต้องรอ design-spec — UI ใหม่ทั้งหมด)
- [ ] T101: สร้าง `NewsScreen.tsx` — list ข่าวพร้อม pull-to-refresh, loading state ที่ชัดเจนระหว่างดึงข้อมูลครั้งแรกที่ยังไม่มี cache ให้แสดง **[รอ design-spec]** (อ้างอิง US-29 AC3,4) — dependency: T100, T92
- [ ] T102: News item card — รูปปก/placeholder, หัวข้อข่าว, สรุปสั้นที่ตัด HTML แล้ว, วันที่เผยแพร่รูปแบบอ่านง่าย **[รอ design-spec]** (อ้างอิง US-29 AC2, US-32 AC2) — dependency: T101
- [ ] T103: Empty state ("ยังไม่มีข่าวในขณะนี้" เมื่อ feed คืนรายการว่างเปล่าจริง) แยกออกจาก error state (ดึงล้มเหลว+ไม่มี cache เดิม, มีปุ่ม/ทางลองดึงใหม่ในหน้าเดิมทันที — reuse pattern เดียวกับ `SearchPlaceField.tsx`/T87-T89 ของ US-25 ตามที่คำตัดสิน PM ข้อ 18 เคยวางไว้) **[รอ design-spec]** (อ้างอิง US-30 AC3, US-32 AC1,4) — dependency: T101, T100
- [ ] T104: Cache indicator แสดงเมื่อกำลังโชว์ข่าวจาก cache (offline หรือ fetch ล้มเหลวแต่มี cache เดิม) เช่น "อัปเดตล่าสุด: ..." จาก timestamp ของ T100 **[รอ design-spec]** (อ้างอิง US-30 AC2) — dependency: T100, T101

#### US-31: In-App Browser (รอ T100 มีข้อมูล/link ให้กดก่อน แต่ไม่ต้องรอสาย Navigation เสร็จ)
- [ ] T105: Wire `expo-web-browser` (`openBrowserAsync`) เมื่อแตะรายการข่าวใดๆ ใน list → เปิด `link` ต้นทางทันที ไม่ทำ full article parsing เอง (อ้างอิง US-31 AC1) — dependency: T91, T102
- [ ] T106: จัดการกรณี `link` ว่างเปล่า/parse ผิดพลาด — ต้องไม่ crash แต่แสดงข้อความแจ้งว่าเปิดข่าวนี้ไม่ได้แทน (อ้างอิง US-31 AC3) — dependency: T105
  - หมายเหตุ (คำตัดสิน PM ข้อ 23): ใช้ toast แบบ transient auto-dismiss ตามที่ design-spec ออกแบบ (Option A) แต่ปรับ duration เป็น **~4 วินาที** (ยาวขึ้นจาก ~2.5 วินาทีที่ design-spec เสนอไว้เดิม) เพื่อลดความเสี่ยงผู้ใช้พลาดข้อความ
- [ ] T107: จัดการกรณีไม่มีเน็ต ณ ตอนกดข่าว — แสดงข้อความแจ้งว่าต้องมีอินเทอร์เน็ตเพื่ออ่านเนื้อหาเต็ม แทนที่จะเปิด browser ค้างเป็นหน้าขาว/error ของเบราว์เซอร์ (ใช้วิธี opportunistic network check แบบเดียวกับ T73/`SyncContext.tsx` เดิม) (อ้างอิง US-31 AC4) — dependency: T105
  - หมายเหตุ (คำตัดสิน PM ข้อ 23): เช่นเดียวกับ T106 — ใช้ toast แบบ transient auto-dismiss (Option A) duration **~4 วินาที** ไม่ใช่ persistent banner
- [ ] T108: ยืนยันว่าปิด in-app browser แล้วกลับมาที่รายการข่าวเดิม scroll position และสถานะรายการยังอยู่เหมือนก่อนกด ไม่ reset กลับไปบนสุด (ต้อง verify จริงเพราะ `openBrowserAsync` เป็น native modal overlay ไม่ unmount screen เดิม แต่ต้องตรวจสอบว่า list component ไม่ re-render/reset state เองระหว่างนั้น) (อ้างอิง US-31 AC2) — dependency: T105

#### US-32: Edge Case Verification (รวมของทั้งชุด — ส่วนใหญ่ implement แล้วใน T97-T104 ด้านบน แต่ต้องมี task ยืนยันแยกชัดเจนไม่ให้ตกหล่น)
- [ ] T109: Regression/edge-case verification รวมของ US-32 ทั้ง 5 ข้อ ต้องมี test แยกแต่ละเคสชัดเจนไม่ปนกัน (เทียบ pattern เดียวกับหมายเหตุ testability ของ T89): (1) feed ว่างเปล่า → empty state ไม่ใช่ error state, (2) ข่าวไม่มีรูป → placeholder ไม่ใช่พื้นที่ว่าง/broken image, (3) ข่าวซ้ำ (`link`/`title` ซ้ำ) → เหลือรายการเดียว, (4) fetch ล้มเหลวจริงไม่มี cache → error state พร้อมปุ่มลองใหม่ในหน้าเดิม, (5) ไม่มี `pubDate` → ยังแสดงในรายการแต่จัดไว้ท้ายสุดไม่ทำให้ทั้งลิสต์ error (อ้างอิง US-32 ทั้งหมด) — dependency: T97, T98, T99, T100, T103

### รอบ 14: Bug Fix รูปภาพข่าวไม่แสดง (US-33)
> คำตัดสิน PM ข้อ 24 (ดูด้านล่าง) เลือกทางเลือก (ข) fetch Open Graph image แบบ lazy เป็นหลัก โดยมี T113 เป็น verification gate และ T116 เป็น contingency path ไปทางเลือก (ก) ถ้า (ข) พิสูจน์ว่าทำไม่ได้จริงในทางปฏิบัติ
- [ ] T113: **[ทำก่อนสุดของสายนี้]** Verify ทางเทคนิคก่อนเขียนโค้ดจริง — สุ่ม fetch หน้าบทความจริงอย่างน้อย ~10 รายการล่าสุดจากแหล่งข่าวที่ระบุไว้ใน `docs/dev-notes.md` ปัจจุบัน (ผลจาก T90/คำตัดสิน PM ข้อ 19) แล้วตรวจสอบว่ามี `<meta property="og:image">` จริงในสัดส่วนที่มีนัยสำคัญหรือไม่ พร้อมวัด response time โดยประมาณ (เพื่อประกอบการตัดสินใจว่า lazy fetch ต่อรายการจะหน่วงเกินไปหรือไม่) — เก็บผลสรุปไว้ใน `docs/dev-notes.md` ต่อท้ายรายการเดิม (อ้างอิง US-33 AC1,2) — dependency: ไม่มี
- [ ] T114: สร้างโมดูล fetch Open Graph image แบบ lazy (เช่น `src/services/ogImageFetcher.ts`) — ดึง HTML ของหน้าบทความจาก `link` แล้ว extract ค่า `og:image` ด้วยวิธี regex/string-based เดียวกับแนวทาง parser เดิม (คำตัดสิน PM ข้อ 21 ไม่ใช้ DOMParser), มี timeout สั้น (เช่น ~5 วินาที) ต่อรายการ, ห่อ try/catch แยกอิสระต่อข่าวแต่ละรายการ (ความล้มเหลวของรายการหนึ่งต้องไม่กระทบรายการอื่นและไม่ throw ออกไปทำลายการแสดงผลหลัก), cache ผลลัพธ์ต่อ `link` แบบเดียวกับ TTL 45 นาทีของ cache ข่าวเดิม (T100) เพื่อไม่ fetch ซ้ำข่าวเดิมทุกครั้งที่ re-render, และออกแบบให้ trigger แบบ **lazy ต่อรายการที่กำลังจะแสดงผลจริงเท่านั้น** (เช่น fetch ตอน `NewsCard` mount/`onViewableItemsChanged` ของ FlatList) ไม่ fetch พร้อมกันทั้งหมดของทั้ง feed ในครั้งเดียว (อ้างอิง US-33 AC1,4) — dependency: T113, T97, T100
- [ ] T115: อัปเดต `NewsCard.tsx`/list rendering ให้ใช้รูปจาก T114 เป็นรูปหลักเมื่อ field รูปจาก RSS parser เดิม (T98) ว่างเปล่า — ต้องไม่บล็อกการแสดง title/summary/date ที่ต้องขึ้นทันทีตาม US-29 เดิม (แสดงก่อน แล้วค่อยสลับรูปเข้ามาเมื่อ fetch เสร็จ), เมื่อไม่มีทั้ง native image field และ og:image (หรือ fetch ล้มเหลว/timeout) ต้องยังคงแสดง placeholder icon เดิมของ US-32 ทุกประการ ไม่ error ไม่ใช่พื้นที่ว่าง/broken image (อ้างอิง US-33 AC1,3,4) — dependency: T114, T102
- [ ] T116: **Contingency — ทำเฉพาะกรณี T113 พิสูจน์ว่าทางเลือก (ข) ทำไม่ได้จริงในทางปฏิบัติ** (เช่น og:image หายากผิดคาด/timeout สูงเกินไป/เว็บบล็อกการ fetch เป็นส่วนใหญ่ของตัวอย่าง) — ให้กลับไปพิจารณาทางเลือก (ก): ตรวจสอบว่าแหล่งใดในลำดับ fallback เดิม (คำตัดสิน PM ข้อ 19: tatnews.org → Bangkok Post travel → The Nation → อื่นๆ) มีรูปแนบจริงในตัว RSS item (`<enclosure>`/`<media:content>`/`<img>` ใน description) แล้วสลับแหล่งข่าวหลักไปยังแหล่งนั้น พร้อมปรับ/ทดสอบ `newsRssParser.ts` (T98) ให้ parse ครบทุก field เดิม (title, summary, image, link, pubDate) จากแหล่งใหม่ถูกต้อง, log แหล่งที่เปลี่ยนไปใช้และเหตุผลไว้ใน `docs/dev-notes.md` — ถ้า T113 สรุปว่าทางเลือก (ข) ใช้ได้จริง ให้ข้าม task นี้และบันทึกใน `docs/dev-notes.md` ว่า "ไม่จำเป็นต้องใช้ทางเลือก (ก)" (อ้างอิง US-33 AC5) — dependency: T113
- [ ] T117: Test + วัดผลตาม AC — เขียน test ด้วย mocked RSS/HTML response ที่มี `og:image`/รูปแนบจริง (ตามทางเลือกที่ใช้จริง) ยืนยันว่าการ์ดที่ render แสดง URL รูปตรงกับ mock ไม่ใช่ placeholder, เขียน test แยกกรณี fetch รูปล้มเหลว/timeout ยืนยันว่ายัง fallback เป็น placeholder ได้โดยไม่ crash และไม่กระทบข่าวรายการอื่น, และรันจริงกับข่าวล่าสุดเพื่อรายงานตัวเลขจริง ("X/Y ข่าวล่าสุดมีรูปแสดงผล") แนบไว้ใน `docs/dev-notes.md`/test evidence ตามที่ AC บังคับ (อ้างอิง US-33 AC2,6) — dependency: T115, T116

## P1 (ควรมี)
- [ ] T22: กันหน้า 2D validation (จาก T6/T7) ไม่ให้ปรากฏใน production build/nav ของ end-user (ทำเป็น dev-only flag หรือ route ซ่อน) (อ้างอิง US-7) — dependency: T7
- [ ] T23: Legend อธิบายสี locked vs unlocked ใต้แผนที่ 3 มิติ **[รอ design-spec]** (อ้างอิง US-1) — dependency: T9
- [ ] T24: หน้าสถิติ: จำนวนจังหวัด unlocked (เทียบฐาน 76) และจำนวน entry ทั้งหมด ตรงกับ local storage จริง **[รอ design-spec]** (อ้างอิง US-6) — dependency: T5, T14
- [ ] T25: หน้าสถิติ: คำนวณภาคที่มีจังหวัด unlocked มากที่สุด + แปลชื่อภาคเป็นไทย (เหนือ/อีสาน/กลาง/ตะวันออก/ตะวันตก/ใต้) (อ้างอิง US-6) — dependency: T5
- [ ] T26: หน้าสถิติ: timeline entry ทั้งหมดจากทุกจังหวัด เรียง reverse-chronological ระบุจังหวัดของแต่ละรายการ **[รอ design-spec]** (อ้างอิง US-6) — dependency: T14
- [ ] T27: หน้าสถิติ: empty state ที่เหมาะสมเมื่อยังไม่มี entry เลย (ไม่มี NaN/undefined) **[รอ design-spec]** (อ้างอิง US-6) — dependency: T24
- [ ] T28: แก้ไข journal entry ที่มีอยู่ (UI + save) **[รอ design-spec]** — dependency: T14
- [ ] T29: ลบ journal entry ได้ ถ้าเป็น entry สุดท้ายของจังหวัดนั้น ต้อง lock จังหวัดกลับ — dependency: T14, T9
- [ ] T54: Error handling UI สำหรับ email-link ล้มเหลว (email ซ้ำ, รหัสผ่านไม่ผ่านเงื่อนไข) แสดงข้อความสื่อความหมาย และ anonymous session เดิมยังใช้ต่อได้ **[รอ design-spec]** (อ้างอิง US-11) — dependency: T38, T49
- [ ] T55: Migration retry/resume handling สำหรับกรณีล้มเหลวบางส่วน (เช่นเน็ตหลุดกลาง migrate) — ครอบคลุม edge case ของ US-15 AC3 ให้ครบถ้วนกว่า T42 พื้นฐาน — dependency: T42
- [ ] T56: Photo upload retry ที่ทนต่อการปิด-เปิดแอประหว่างคิวยังไม่เสร็จ (คิวต้อง persist ข้าม session) (อ้างอิง US-13) — dependency: T41
- [ ] T57: Dev-only debug view/log สำหรับตรวจสอบพฤติกรรม last-write-wins ตอน conflict จริง (ไม่ใช่หน้าที่ end-user เห็น) — dependency: T40
- [ ] T61: รันสคริปต์จาก T60 จริงกับ Overpass API (`overpass-api.de`) เพื่อ generate ข้อมูล landmark จริง (เท่าที่ query ได้) ครบทั้ง 76 จังหวัด — ความเสี่ยง: ไม่ยืนยันว่าสภาพแวดล้อมนี้มี outbound network access ไปยัง `overpass-api.de` ได้จริงหรือไม่ ถ้ายิงไม่ได้ ให้ programmer fallback สร้าง mock/sample Overpass response (เช่นข้อมูลตัวอย่าง 3–5 จังหวัด) เพื่อพิสูจน์ว่า pipeline T60→T64 ทำงานถูกต้องก่อน แล้ว flag ไว้ชัดเจนในผลงานว่า "ยังไม่ได้รันจริงครบ 76 จังหวัด ผู้ใช้ต้องรันสคริปต์เองภายหลังเมื่อมี network ออกนอกได้จริง เพื่อ generate ข้อมูลจริงครบทุกจังหวัด" (อ้างอิง US-16) — dependency: T60 (ดูคำตัดสิน PM ข้อ 13)
- [ ] T110: Accessibility pass สำหรับ NewsScreen/news item card (`accessibilityLabel`/`accessibilityRole` ตาม pattern เดียวกับ `LandmarkCard.tsx` ที่มี `accessibilityRole="switch"`) — ไม่ใช่ AC บังคับตรงๆ ของ US-29-US-32 แต่เพื่อความสอดคล้องกับมาตรฐานที่แอปวางไว้แล้ว (อ้างอิง US-29) — dependency: T102
- [ ] T111: เพิ่ม test case ให้ parser (T98) ครอบคลุมรูปแบบ RSS ที่พบได้จริงเพิ่มเติมนอกเหนือจาก mock ขั้นต่ำของ AC (เช่น ค่าที่ห่อด้วย `CDATA`, namespace prefix อย่าง `media:content`/`atom:link`, HTML entity ใน title/description) — ลดความเสี่ยงที่ parser จะพังจริงเมื่อเจอ RSS source จริงที่มีรูปแบบหลากหลายกว่าตัวอย่างขั้นต่ำ (อ้างอิง US-29 AC5) — dependency: T98

### รอบ 14: ยกระดับ Animation/Motion (US-34) — ต้องรอ UIUX ออกแบบ motion spec ก่อนเริ่มโค้ด
- [x] T118: **[ต้องมี UIUX ออกแบบก่อน — ส่งต่อ orchestrator ให้เรียก UIUX]** ออกแบบ motion/micro-interaction spec ฉบับใหม่ ต่อท้ายใน `docs/design-spec.md` ครอบคลุมอย่างน้อย 3 ส่วนตาม AC ของ US-34: (1) รูปแบบ screen transition ที่ใช้ตอนสลับแท็บ "แผนที่"/"ข่าว", เปิด ProvinceDetailScreen, เปิด AddEntryScreen (ระบุชนิด เช่น slide/fade และทิศทาง/duration ที่จับต้องได้), (2) รูปแบบ entrance animation ของ list หลัก (Landmark list, News list, Stats timeline) เช่น stagger fade-in/slide-in พร้อมค่า delay/duration ต่อ item, (3) ค่าพารามิเตอร์ใหม่ของ press feedback บนปุ่ม/การ์ดสำคัญ (ปุ่มเช็คอิน, การ์ด Landmark, การ์ดข่าว, ปุ่ม "+ เพิ่มบันทึกใหม่") ที่ต้องรู้สึกต่างชัดเจนกว่า PressableScale ปัจจุบัน — ต้องระบุค่าที่ programmer เอาไปเขียนโค้ดตรงได้ทันทีโดยไม่ต้องเดา (อ้างอิง US-34 AC1,2,3) — dependency: ไม่มี
- [x] T119: ปรับ screen transition config ใน `AppNavigator.tsx` (native-stack `screenOptions`/`animation`, bottom-tabs transition) ให้มี custom transition ที่มองเห็นชัดครบ 3 จุดตาม motion spec จาก T118 (สลับแท็บ, เปิด ProvinceDetailScreen, เปิด AddEntryScreen) — ใช้ไลบรารีที่มีอยู่แล้วในโปรเจกต์ (`react-native-reanimated`, native-stack built-in animation options) ห้ามเพิ่มไลบรารี animation ใหม่ตาม Out of Scope ของ US-34 (อ้างอิง US-34 AC1) — dependency: T118
- [x] T120: เพิ่ม entrance animation ให้ list หลักครบ 3 จุดตาม AC: `LandmarkList.tsx` (รายการ Landmark ในหน้าจังหวัด), list ข่าวใน `NewsScreen.tsx` (T101), timeline entry ในหน้าสถิติ (T26) — fade-in/slide-in ทีละรายการหรือเป็นกลุ่มตอนข้อมูลโหลดเสร็จและ render ครั้งแรก ตามค่าพารามิเตอร์จาก T118 โดยใช้ `Animated`/`react-native-reanimated` ที่มีอยู่แล้ว ต้องไม่บังคับให้ผู้ใช้รอ animation จบก่อนโต้ตอบกับรายการได้ (อ้างอิง US-34 AC2,5) — dependency: T118
  - หมายเหตุ (คำตัดสิน PM ข้อ 25): ขยายขอบเขต T120 เพิ่มจุดที่ 4 นอกเหนือจาก 3 จุดขั้นต่ำของ AC — HomeScreen 3D map: ให้ grid container ของ 76 tiles ทั้งก้อน (ไม่ใช่ทีละ tile) เล่น single-opacity fade-in ครั้งเดียวตอนเข้าหน้า Home (~250–300ms) ควบคู่กับ header/legend (จำนวน unlocked, progress bar) fade-in แยกต่างหาก — **ห้าม animate ทีละ tile หรือ animate ตำแหน่ง/scale ของ tile ใดๆ เพิ่มเติมเด็ดขาด** เพื่อคุม performance risk ให้อยู่ในระดับต่ำที่สุด — **Safety net บังคับ**: ต้องทดสอบบนอุปกรณ์จริงอย่างน้อย 1 เครื่องระดับกลาง/ล่างก่อนปิดงาน ถ้าพบ jank/หน่วงที่สังเกตได้ระหว่างเล่น fade-in ของ grid container ให้ fallback ทันทีเหลือแค่ header/legend fade-in (ตัด grid container animation ออก) โดยไม่ต้องกลับมาขอ PM ตัดสินใจซ้ำ แล้วบันทึกผลการทดสอบ/เหตุผลไว้ใน `docs/dev-notes.md`
- [x] T121: ปรับปรุง visual feedback ตอนกดของปุ่ม/การ์ดสำคัญ (ปุ่มเช็คอินใน `LandmarkCard.tsx`, การ์ด Landmark, การ์ดข่าวใน `NewsCard.tsx`, ปุ่ม "+ เพิ่มบันทึกใหม่") ให้ชัดเจนกว่า PressableScale เดิมตามค่าพารามิเตอร์ใหม่จาก T118 (เช่น scale/opacity/shadow ที่แรงขึ้นอย่างมีนัยสำคัญ ไม่ใช่ปรับตัวเลขเล็กน้อยจนสังเกตไม่ออก) — ต้องยัง smooth ไม่ jank บนอุปกรณ์ทั่วไป (อ้างอิง US-34 AC3) — dependency: T118
- [x] T122: Regression guard รวมของ US-34 — ยืนยันว่า functional behavior เดิมทั้งหมดของ US-1 ถึง US-32 (ข้อมูลที่แสดง, ผลลัพธ์ของการนำทาง, ผลของการกดปุ่ม/action ต่างๆ) ไม่เปลี่ยนแปลงหลังเพิ่ม animation จาก T119–T121 ทั้งหมด และ animation ทุกจุดสามารถกดข้าม/interrupt ได้เสมอ ไม่บังคับผู้ใช้รอ animation เล่นจบก่อนกดปุ่มถัดไป (อ้างอิง US-34 AC4,5) — dependency: T119, T120, T121
- [ ] T123: เขียน/ปรับ test ยืนยันว่า animation config/prop ที่เกี่ยวข้อง (transition config ของ navigator จาก T119, prop ของ `Animated`/`react-native-reanimated` ใน list จาก T120 และใน element กดจาก T121) ถูกเพิ่ม/เปลี่ยนแปลงจริงในโค้ดของแต่ละหน้าจอที่ระบุใน AC ไม่ใช่แค่คงค่า default เดิมไว้เฉยๆ (อ้างอิง US-34 AC6) — dependency: T119, T120, T121

## P2 (ดีถ้ามี)
- [ ] T30: ทดสอบ/ปรับ performance การ render SVG 76 จังหวัดบนอุปกรณ์สเปคต่ำ (โดยเฉพาะระหว่างอนิเมชัน T16) — dependency: T16
- [x] T31: App icon และ splash screen **[รอ design-spec]** — dependency: T1
  - หมายเหตุ (post-QA fix, นอก pipeline รอบ 1): สร้าง `assets/icon.png`, `assets/splash-icon.png`, `assets/favicon.png`, `assets/android-icon-*.png` ใหม่ด้วย `jimp-compact`/`pngjs` (มีอยู่แล้วใน node_modules) แทนไฟล์ default ของ Expo — พื้นหลัง solid `#1D9E75` + สัญลักษณ์หมุดแผนที่สีขาวพื้นฐาน (ไม่มี custom illustration) ตรงตาม AC เดิม, แก้ `android.adaptiveIcon.backgroundColor` ใน `app.json` จาก `#E6F4FE` (ค่า default เดิม ไม่ตรงธีม) เป็น `#1D9E75` ด้วย — ยังไม่ได้ wiring `expo-splash-screen` plugin (ไม่มี dependency นี้ติดตั้ง, อยู่นอกขอบเขต AC ของ T31 ซึ่งพูดถึงแค่เนื้อหาภาพ)
  - AC (จากคำตัดสิน PM ข้อ 4): ใช้สีธีมหลัก (#1D9E75 accent / #0F6E56) เป็น icon และ splash แบบเรียบง่าย (solid color + สัญลักษณ์/ตัวอักษรพื้นฐาน) ไม่ต้องมี custom illustration ใน v1 — อยู่ใน scope v1 แต่เป็น P2 ไม่บล็อกการ build ฟีเจอร์หลัก
- [ ] T58: Performance test สำหรับ sync engine เมื่อมี pending queue ขนาดใหญ่ (entries + photos จำนวนมากพร้อมกัน เช่นตอน migrate ข้อมูล v1 เก่า) ต้องไม่บล็อก UI — dependency: T39, T41
- [ ] T59: จัดทำ checklist/กระบวนการสำหรับทีม content คัดสรร landmark เพิ่มให้ครบทั้ง 76 จังหวัดในรอบถัดๆ ไป (ไม่ใช่งานโค้ด ไม่บล็อก sprint นี้) — dependency: T35
- [ ] T75: ผนวกรายชื่อจังหวัดที่ Overpass คืนผลลัพธ์ไม่ครบ 3 แห่ง (จาก log ของ T60/T61) เข้ากับ checklist การคัดสรร content เดิมของ T59 เพื่อให้ทีม content ทำ manual review เพิ่มเติมต่อ (นอก sprint พัฒนาโค้ด) (อ้างอิง US-16 AC5, Out of Scope รอบ OSM) — dependency: T59, T61
- [ ] T112: Performance/list tuning สำหรับ NewsScreen ถ้าจำนวนข่าวใน feed มีมาก (FlatList windowing/`getItemLayout` ฯลฯ) — ไม่มี AC ระบุจำนวนสูงสุดของข่าวต่อ feed จึงเป็นงานเสริมไม่บล็อก sprint นี้ — dependency: T101

## คำตัดสิน PM (ถ้ามี)

### ประเด็น 1: ไม่มี label จังหวัดบนแผนที่ vs โอกาส "สำรวจ" ก่อน navigate
- คำตัดสิน: เลือกทางเลือก (b) — เพิ่ม long-press เพื่อแสดง tooltip ชื่อจังหวัดชั่วคราวก่อน tap สั้นเพื่อ navigate (เพิ่ม T32)
- เหตุผล: ยังคงไม่มี label ถาวรบนแผนที่ตาม requirement เดิม (ไม่ผิดเป้าหมายเรื่องความรกของ 76 label) แต่แก้ปัญหา UX จริงที่ UIUX ชี้ — ผู้ใช้ควรสำรวจแผนที่ได้โดยไม่ต้อง navigate เข้า-ออกทุกครั้งเพื่อรู้ว่า tile คือจังหวัดไหน ต้นทุนเพิ่ม (gesture handler เดียว) ต่ำเทียบกับคุณค่าที่ได้ ไม่กระทบ timeline อย่างมีนัยสำคัญ
- **ต้องอัป requirements**: ใช่ — requirements.md ต้องเพิ่ม AC ใหม่ใน US-2 หรือ US-1 ระบุ long-press = แสดง tooltip ชื่อจังหวัด (ไม่ navigate), tap สั้น = navigate ตามเดิม

### ประเด็น 2: Performance risk ของ 76 tile + animation
- คำตัดสิน: ไม่ต้องตัดสินใจระดับ PM — เป็นข้อสังเกตทางเทคนิคส่งต่อ programmer ตามที่ UIUX ระบุแล้ว ให้ยึด T30 (P2) เป็นที่เก็บงานนี้ต่อไป และย้ำผ่าน T8/T9 ว่าต้อง isolate re-render เฉพาะ tile ที่ state เปลี่ยน
- เหตุผล: ไม่กระทบ requirement/design ใดๆ เป็นรายละเอียด implementation
- ต้องอัป requirements: ไม่ต้อง

### ประเด็น 3: Tag set ปิดหรือเปิดให้ custom
- คำตัดสิน: เลือกทางเลือก (b) แบบผสม — fixed tag set เริ่มต้น 8 แท็ก (ทะเล/ภูเขา/วัด/คาเฟ่/น้ำตก/เมือง/ธรรมชาติ/อาหาร) เป็น multi-select chip พร้อมให้ผู้ใช้พิมพ์ custom tag เพิ่มเองได้
- เหตุผล: แอปนี้เป็น personal travel journal ที่เน้นความทรงจำเฉพาะบุคคล fixed list อย่างเดียวจะจำกัดการบันทึกของผู้ใช้ในระยะยาว (เช่น ทริปที่ไม่เข้าพวก 8 แท็กเลย) ส่วนต้นทุนเพิ่ม (text input + เก็บ tag string ใน sqlite) ต่ำ ไม่กระทบ timeline มาก เพราะ storage เก็บ tags เป็น array/string อยู่แล้วตาม data model เดิม
- **ต้องอัป requirements**: ใช่ — requirements.md ต้องปิด tag list เป็นรายการ 8 แท็กที่ระบุชัด และเพิ่ม AC ว่าอนุญาต custom tag ที่ผู้ใช้พิมพ์เพิ่มได้ (ปัจจุบันเขียนแค่ "เช่น ทะเล/ภูเขา/วัด/คาเฟ่" แบบเปิดกว้างไม่ชัดเจน)

### ประเด็น 4: T31 (app icon/splash) ไม่มี AC
- คำตัดสิน: คงอยู่ใน scope v1 เป็น P2 แต่ทำแบบเรียบง่ายที่สุด — ใช้สีธีมหลัก (#1D9E75/#0F6E56) เป็น solid background + สัญลักษณ์/ตัวอักษรพื้นฐานเท่านั้น ไม่ทำ custom illustration
- เหตุผล: ตัดออกจาก scope ทั้งหมดจะทำให้แอปไม่มี icon ใน production build เลยซึ่งดูไม่สมบูรณ์แม้เป็น v1 ภายใน แต่การลงทุนออกแบบ icon ที่ซับซ้อนก็ไม่คุ้มเวลาเทียบกับ core features (P0/P1) จึงเลือกทางสายกลางที่ทำเร็วและได้ผลลัพธ์พอใช้
- **ต้องอัป requirements**: ใช่ — requirements.md ไม่เคยพูดถึง icon/splash เลยแม้แต่ใน Out of Scope ต้องเพิ่มเป็น AC สั้นๆ ใน scope (หรือ note ใน สมมติฐาน) ว่า v1 มี icon/splash แบบ solid-color-only ตามที่ตัดสินนี้ เพื่อไม่ให้ค้างเป็นช่องว่างที่ไม่มีเอกสารอ้างอิง

### ประเด็น 5 (รอบ Landmark & Supabase): ขอบเขต Landmark seed dataset — เริ่มจากจังหวัดนำร่องหรือรอ curate ครบ 76 จังหวัด
- คำตัดสิน: ชิปแบบ functionally-complete ทันที — สร้าง `thailand-landmarks.ts` ครอบคลุมจังหวัดนำร่อง 8 จังหวัด (เสนอ: กรุงเทพมหานคร, เชียงใหม่, เชียงราย, พระนครศรีอยุธยา, สุโขทัย, ชลบุรี, กระบี่, ภูเก็ต) จังหวัดละ 3–5 landmark จริง ส่วนอีก 68 จังหวัดที่เหลือส่ง array ว่าง/TBD และให้ empty state (US-8 AC4) รับผิดชอบแทน ไม่บล็อกงาน UI/data layer/QA ของทั้งฟีเจอร์ไว้รอ content ครบ 76 จังหวัด
- เหตุผล: requirements.md ระบุไว้แล้วว่า "ในรอบแรกอาจเริ่มจากจังหวัดนำร่องบางส่วนก่อน แล้วทยอยเพิ่มให้ครบ" — งาน curate เนื้อหาจริงครบ 76 จังหวัดเป็นงาน content แยกที่ต้องใช้เวลาและกระบวนการตรวจสอบข้อมูลต่างหาก ถ้าบล็อก UI ไว้รอ content ครบ จะทำให้ QA ทดสอบ US-8/US-9/US-10 ทั้งหมดไม่ได้เลยในรอบนี้ การมี pilot dataset ควบคู่ empty state ที่ถูกต้องทำให้ทุก AC ทดสอบได้จริงตั้งแต่ต้น และ empty state เองก็เป็น AC บังคับอยู่แล้วใน US-8
- ต้องอัป requirements: ไม่ต้อง (เป็นการดำเนินการตามสมมติฐานที่ระบุไว้แล้วในบรรทัด "สมมติฐานเพิ่มเติม" ข้อ 2 ของ requirements.md ไม่ได้เปลี่ยน scope หรือ AC ใดๆ)

### ประเด็น 6 (รอบ Landmark & Supabase): จุด/รูปแบบคำเตือนความเสี่ยงข้อมูลหาย (US-11)
- คำตัดสิน: กำหนด non-negotiable constraint ให้ UIUX ออกแบบภายใต้กรอบนี้ (รายละเอียดภาพ/wording/ตำแหน่งที่แน่นอนเป็นดุลยพินิจ UIUX):
  1. ต้องมี one-time prominent prompt/modal แสดงทันทีหลังสร้าง anonymous session สำเร็จครั้งแรก (ตาม US-11 AC1)
  2. ต้องมีจุดที่ "มองเห็นได้เสมอ" อย่างน้อย 1 จุด (เช่นหน้าตั้งค่า/โปรไฟล์) ตราบใดที่ session ยังเป็น anonymous ไม่ผูก email — ห้าม silently absent
  3. ถ้าใช้รูปแบบ dismissible banner/card การ dismiss ต้องไม่ถาวร — ต้อง re-surface อย่างน้อยทุก 7 วัน หรือทุกครั้งที่ผู้ใช้เปิดหน้าตั้งค่า/โปรไฟล์ (เลือกอย่างใดอย่างหนึ่ง)
  4. ห้ามมีทางลบคำเตือนออกถาวรด้วยการกดปุ่มเดียวโดยไม่มีกลไก re-surface ใดๆ เลย
- เหตุผล: ความเสี่ยงข้อมูลหายถาวรเป็น hard requirement ทางธุรกิจ (BA ระบุใน US-11 AC3) ต้องมี floor ที่ไม่ต่อรองเพื่อกันไม่ให้ UIUX ออกแบบคำเตือนแบบเนียนจนมองไม่เห็นหรือปัดทิ้งถาวรได้ง่ายเกินไป ส่วนรูปแบบภาพเป็นงาน UX โดยแท้จึงปล่อยให้ UIUX ตัดสินใจในกรอบนี้
- ต้องอัป requirements: ไม่ต้อง (requirements.md ส่งเรื่องรูปแบบคำเตือนมาให้ PM/UIUX ตัดสินใจอยู่แล้วตาม AC เดิมของ US-11 ข้อ 3 ไม่ได้เปลี่ยน scope)

### ประเด็น 7 (รอบ Landmark & Supabase): Conflict resolution — ยอมรับ last-write-wins แบบ whole-record ตามที่ BA เสนอ
- คำตัดสิน: ยอมรับข้อเสนอ BA ทั้งหมด ใช้ last-write-wins ทั้ง record โดยเทียบ `updated_at` เป็นขอบเขต P0 ของรอบนี้ (T40) — **field-level merge อยู่นอกขอบเขต (out of scope) อย่างชัดเจนสำหรับรอบนี้**
- เหตุผล: field-level merge ต้องมี schema เปรียบเทียบระดับ field และ UI ให้ผู้ใช้เลือกฝั่งที่จะเก็บ ซึ่งเพิ่มความซับซ้อนสูงเทียบกับคุณค่าที่ได้ในรอบที่ยังทดสอบด้วย mock Supabase client เท่านั้น (ยังไม่มี production sync จริงที่จะเจอ conflict บ่อยหรือรุนแรง) เหมาะเก็บไว้พิจารณาในรอบถัดไปถ้าพบปัญหาจริงจากการใช้งานหลายเครื่องจริง
- ต้องอัป requirements: ไม่ต้อง (requirements.md ระบุ last-write-wins และ exclude field-level merge ไว้ใน Out of Scope อยู่แล้ว ตรงกับคำตัดสินนี้พอดี)

### ประเด็น 8 (รอบ Landmark & Supabase): Dependency สำหรับ Supabase — ตรวจสอบพบว่าติดตั้งไว้แล้ว
- คำตัดสิน: ตรวจสอบ package.json แล้วพบว่า `@supabase/supabase-js` (^2.116.0), `@react-native-async-storage/async-storage` (2.2.0), และ `react-native-url-polyfill` (^4.0.0) ถูกติดตั้งไว้แล้วทั้งหมด — ปรับ T33 จาก "ติดตั้ง dependency ใหม่" เป็น "ยืนยันเวอร์ชัน + เขียน Supabase client init module (env-based `SUPABASE_URL`/`SUPABASE_ANON_KEY`, ใช้ mock/local client ตามสมมติฐานที่ระบุไว้)" แทน ไม่มี task ติดตั้ง package ซ้ำซ้อน
- เหตุผล: ป้องกัน programmer เสียเวลาไปรันคำสั่งติดตั้งที่ไม่จำเป็นหรือเข้าใจผิดว่ายังไม่มี dependency ในโปรเจกต์
- ต้องอัป requirements: ไม่ต้อง (เป็นรายละเอียด implementation ไม่กระทบ scope/AC ใดๆ)

### ประเด็น 9 (รอบ Landmark & Supabase): Sync Status Badge — รับ state ที่ 3 "retry-issue" ตามที่ design-spec เสนอ
- คำตัดสิน: รับทางเลือก B — ให้มี 3 สถานะ "รอซิงก์" / "ซิงก์แล้ว" / "retry-issue" (sync ล้มเหลวต่อเนื่องหลายครั้ง) แทนที่จะตัดกลับเหลือ 2 สถานะขั้นต่ำ ปรับ T39 ให้รวม logic นับจำนวนครั้งที่ sync ล้มเหลวติดต่อกันต่อ record เพื่อ trigger state นี้ และปรับ T51 ให้ badge/ไอคอนรองรับ 3 สถานะ
- เหตุผล: AC เดิมของ US-12 เขียนว่า "อย่างน้อยในระดับที่ไม่ทำให้ผู้ใช้เข้าใจผิดว่าข้อมูลหาย" ซึ่งเป็น floor ไม่ใช่ ceiling — state ที่ 3 ไม่ได้ขัดกับ Out of Scope ที่ห้ามเฉพาะ "push notification/แจ้งเตือนสำเร็จ-ล้มเหลว" เพราะยังเป็น in-app status indicator ล้วนๆ ไม่ใช่ notification และไม่ใช่ full activity log effort เพิ่มที่ UIUX ประเมินไว้อยู่ในระดับ "เล็กน้อย" (แค่ counter + threshold) จึงคุ้มค่ากับความโปร่งใสที่ผู้ใช้ได้รับเมื่อเจอปัญหา sync ค้างนานผิดปกติ ไม่มีความเสี่ยงต่อ timeline อย่างมีนัยสำคัญ
- ต้องอัป requirements: ไม่ต้อง (ไม่ขัดกับ AC หรือ Out of Scope เดิมของ US-12 — เป็นการทำเกินขั้นต่ำที่ AC อนุญาตไว้ ("อย่างน้อย") ไม่ใช่การเปลี่ยน scope/เงื่อนไข acceptance)

### ประเด็น 10 (รอบ Landmark & Supabase): Data-loss banner — ยืนยันรับ non-dismissible design
- คำตัดสิน: ยืนยันรับดีไซน์ non-dismissible banner ตามที่ UIUX เลือก (ไม่มีปุ่มปิดเลย แสดงถาวรในหน้าตั้งค่า/โปรไฟล์จนกว่าจะผูก email สำเร็จ) ไม่ต้องปรับเป็น dismissible-with-resurface — ไม่มีการเปลี่ยนแปลงต่อ T50
- เหตุผล: นี่คือทางเลือกที่ตรงตาม constraint ทั้ง 4 ข้อในคำตัดสิน PM ข้อ 6 ได้ง่ายและชัวร์ที่สุดโดยไม่ต้องมี state/logic นับวันหรือ track dismissal เพิ่มเติม ซึ่งลดความเสี่ยง bug และลดงาน QA เทียบกับทางเลือก dismissible+re-surface โดยไม่ได้ประโยชน์เพิ่มเติมด้าน UX ที่ชัดเจนพอจะคุ้มความซับซ้อนที่เพิ่มขึ้น อยู่ในกรอบดุลยพินิจที่ PM มอบให้ UIUX ไว้แล้วตั้งแต่ต้น (ข้อความ PM: "รายละเอียดภาพ/wording/ตำแหน่งที่แน่นอนเป็นดุลยพินิจ UIUX")
- ต้องอัป requirements: ไม่ต้อง (ไม่ใช่ conflict และไม่เปลี่ยน scope/AC — เป็นการยืนยันทางเลือกที่อยู่ในกรอบ constraint เดิมที่ requirements.md/PM decision ข้อ 6 อนุญาตไว้แล้ว)

### ประเด็น 11 (รอบ OSM Data Integration): Nominatim prefill → title field เท่านั้น ไม่สร้าง Landmark ใหม่ ไม่ auto check-in
- คำตัดสิน: ยืนยันรับข้อเสนอของ BA ทั้งหมดตามที่เขียนไว้ใน requirements.md (สมมติฐานเพิ่มเติมรอบ OSM ข้อที่ว่าด้วย Nominatim + US-19 AC3/AC4) — ผลลัพธ์จาก Nominatim ที่ผู้ใช้เลือก prefill เข้าฟิลด์ "title" ของฟอร์ม AddEntryScreen เดิม (US-4) เท่านั้น พร้อมแนบ lat/lng เป็น metadata เสริมบน entry (ผ่านคอลัมน์ใหม่ `placeLat`/`placeLng` ใน T69) — ไม่สร้าง Landmark ใหม่ในตาราง/seed dataset และไม่ trigger auto check-in ตาม US-10 ไม่ว่ากรณีใด แยก mechanism ออกจากกันโดยสมบูรณ์ (T71 เทียบกับ T47/T48)
- เหตุผล: (1) สถานที่จาก Nominatim ยังไม่ผ่านกระบวนการคัดกรองเป็น "Top Landmark" ของจังหวัด (ต่างจาก landmark ใน `thailand-landmarks.ts` ที่คัดสรร/ดึงมาแบบมีเกณฑ์ตาม US-16) การให้มันกลายเป็น Landmark ถาวรหรือ trigger check-in อัตโนมัติจะทำให้ progress "Province Master" (US-9) และรายการ landmark ที่แชร์ระหว่างผู้ใช้ (ถ้ามีในอนาคต) ปนเปื้อนด้วยข้อมูลที่ไม่ได้ผ่านการตรวจสอบคุณภาพเดียวกัน (2) เก็บ auto check-in ไว้เฉพาะ flow ที่มาจาก curated dataset (US-10) ทำให้ semantics ของ "เช็คอิน Landmark" ยังคงมีความหมายชัดเจนสม่ำเสมอ ไม่ใช่ทุกสถานที่ที่เคยพิมพ์ค้นหาจะกลายเป็น "เยือน Landmark" (3) ความซับซ้อนที่เพิ่มขึ้นถ้าให้สร้าง Landmark ใหม่แบบ dynamic (ต้อง generate id, ตรวจสอบ id ซ้ำ, ตัดสินใจว่าจะขึ้น Supabase อย่างไร) ไม่คุ้มกับคุณค่าที่ได้ในรอบนี้ และขัดกับ Out of Scope ที่ BA ระบุไว้ชัดแล้วว่า "ไม่บันทึกผลลัพธ์จาก Nominatim เป็น Landmark ถาวรในระบบ"
- ต้องอัป requirements: ไม่ต้อง (ตรงกับสิ่งที่ requirements.md เขียนไว้แล้วทุกประการ เป็นการยืนยัน ไม่ใช่การเปลี่ยนแปลง)

### ประเด็น 12 (รอบ OSM Data Integration): Reconcile landmark dataset เดิม (8 จังหวัดนำร่อง) กับผลลัพธ์ใหม่จาก Overpass
- คำตัดสิน: เลือก "เติมเสริม" (supplement) — สคริปต์ extraction (T60/T61) และขั้นตอน merge (T64) เติมข้อมูลเฉพาะ 68 จังหวัดที่ยังเป็น array ว่างใน `thailand-landmarks.ts` เท่านั้น **ห้ามแทนที่หรือ regenerate landmark ของ 8 จังหวัดนำร่องเดิมทั้งชุด** ส่วนพิกัด lat/lng ที่ 8 จังหวัดนำร่องยังขาดอยู่ ให้ backfill แยกต่างหาก (T63) โดยคง `id` เดิมทุกตัวไว้ 100% ไม่มีการสร้าง id ใหม่ทับ
- เหตุผล: `src/storage/CheckinContext.tsx` (T36) เก็บสถานะ check-in ของผู้ใช้เป็น `Record<landmarkId, boolean>` โดยอ้างอิงด้วย `landmarkId` string ตรงๆ (เช่น `bkk-grand-palace`) ถ้า id เปลี่ยนหรือ regenerate ใหม่จาก Overpass (ซึ่งมักให้ OSM node/way id ที่ format ต่างไปจาก id เดิมที่ทีมตั้งเอง) สถานะ check-in และความคืบหน้า Province Master ที่ผู้ใช้บันทึกไว้แล้วของ 8 จังหวัดนี้จะหายไปทันทีโดยไม่มีทาง recover — เป็นความเสี่ยง data-loss ที่ยอมรับไม่ได้สำหรับ local-only app ที่ไม่มี backend กู้คืน การเติมเสริมเฉพาะจังหวัดที่ยังไม่มีข้อมูลจึงเป็นทางเลือกเดียวที่ปลอดภัยและตรงกับ AC ของ US-17 ข้อสุดท้ายที่ระบุเงื่อนไขนี้ไว้ตรงๆ อยู่แล้ว
- ต้องอัป requirements: ไม่ต้อง (requirements.md ระบุเงื่อนไขนี้ไว้ชัดเจนแล้วใน US-17 AC5 ว่า "ต้องระบุวิธี reconcile ให้ชัดเจน... โดยไม่ทำให้สถานะ check-in ที่ผู้ใช้เคยบันทึกไว้หายไป" — คำตัดสินนี้เป็นการเลือกวิธีที่สอดคล้อง ไม่ใช่การเปลี่ยน AC)

### ประเด็น 13 (รอบ OSM Data Integration): แยก task เขียนสคริปต์ (T60) ออกจาก task รันสคริปต์จริง (T61) พร้อม fallback
- คำตัดสิน: แยกเป็น 2 task ชัดเจน — T60 (เขียนโค้ด extraction script ที่ทดสอบ/รีวิวได้ ไม่ต้องยิง network จริงตอน dev ก็ตรวจสอบ logic ได้ด้วย unit test/mock response) เป็น **P0** เพราะเป็น deliverable ของโค้ดที่ควบคุมความเสี่ยงได้เต็มที่ ส่วน T61 (รันสคริปต์จริงยิงไปที่ `overpass-api.de` เพื่อ generate ข้อมูลจริงครบ 76 จังหวัด) เป็น **P1** ที่มีความเสี่ยงชัดเจนว่าอาจทำไม่ได้ในสภาพแวดล้อมนี้ถ้าไม่มี outbound network access — กำหนด fallback ไว้ล่วงหน้าใน T61 ให้ใช้ mock/sample Overpass response พิสูจน์ pipeline (T60→T64) ก่อน แล้ว flag ให้ผู้ใช้รันสคริปต์เองภายหลังเพื่อ generate ข้อมูลจริงครบ 76 จังหวัด
- เหตุผล: ต่างจาก T31 (raster icon) ที่ตอนแรกดูเหมือนทำไม่ได้แต่ท้ายที่สุดพิสูจน์ได้ว่าทำได้ด้วย library ที่มีอยู่แล้วในเครื่อง (ไม่มีความเสี่ยงจริง) กรณีนี้ต่างออกไปตรงที่ความเสี่ยงมาจาก **network access ออกนอกเครื่อง** ไปยัง `overpass-api.de` ซึ่งเป็นปัจจัยที่ควบคุมไม่ได้จาก library ภายในเครื่องเพียงอย่างเดียว จึงต้องเผื่อ fallback ไว้ตั้งแต่ต้นแทนที่จะสมมติว่าทำได้แน่นอน การแยก 2 task ทำให้ต่อให้ T61 ทำไม่สำเร็จในรอบนี้ งาน T60 (โค้ดที่รีวิวได้) ก็ยังส่งมอบสำเร็จและพร้อมให้ผู้ใช้รันเองในสภาพแวดล้อมที่มีเน็ตได้ทันที ไม่ทำให้ P0 ทั้งสายอื่น (T62-T74) ติดขัด เพราะทุก task ที่ตามมาออกแบบให้รับ fallback data ได้เช่นกัน
- ต้องอัป requirements: ไม่ต้อง (requirements.md ระบุไว้แล้วว่าสคริปต์นี้เป็น dev/build-time tool และยอมรับว่าบางจังหวัดอาจได้ข้อมูลไม่ครบ — การแยก P0/P1 พร้อม fallback เป็นการวางแผนการส่งมอบ ไม่ใช่การเปลี่ยน scope หรือ AC)

### ประเด็น 14 (รอบ OSM Data Integration): field lat/lng บน Landmark type — ชื่อ field และ optional/required
- คำตัดสิน: ใช้ชื่อ field `lat`/`lng` (ตรงตามคำที่ AC ของ US-16/US-17 เขียนไว้ ไม่ใช้ `latitude`/`longitude` เต็มคำ) และกำหนดเป็น **optional** บน `Landmark` interface (ปัจจุบันประกาศอยู่ใน `src/data/thailand-landmarks.ts` ไม่ใช่ `src/types/landmark.ts`) — ควบคู่กับ T63 ที่ backfill พิกัดให้ 8 จังหวัดนำร่องเดิมในสคริปต์/ขั้นตอนเดียวกันกับการ merge ข้อมูลใหม่ เพื่อให้ landmark ส่วนใหญ่มีพิกัดครบในทางปฏิบัติ แต่ type ยังคง optional ไว้เป็น safety net
- เหตุผล: แม้ตั้งใจ backfill ให้ครบ (T63) แต่ US-18 AC2 เขียนไว้ชัดว่า "Landmark ที่ไม่มีพิกัด...จะไม่แสดงจุดบนแผนที่ แต่ยังคงปรากฏในรายการ list ปกติ...โดยไม่ error" ซึ่งหมายความว่าระบบต้อง **รองรับกรณีไม่มีพิกัดได้ตลอดไป** ไม่ใช่แค่ช่วง transition — ถ้ากำหนดเป็น required จะบังคับให้ทุก landmark ในอนาคต (รวมถึงที่ทีม content เพิ่มเองมือในรอบถัดไปตาม T59) ต้องมีพิกัดเสมอ ซึ่งเพิ่มภาระโดยไม่จำเป็นและขัดกับ AC ที่ออกแบบมาให้ยืดหยุ่นอยู่แล้ว optional field จึงตรงกับทั้ง backward-compat (8 จังหวัดเดิมก่อน backfill) และ forward-compat (landmark ใหม่ที่ยังไม่มีพิกัดในอนาคต) ได้ดีที่สุด
- ต้องอัป requirements: ไม่ต้อง (สอดคล้องกับ US-16 AC3/US-18 AC2 ที่เขียนไว้แล้วว่าต้องรองรับกรณีไม่มีพิกัดโดยไม่ error — ไม่ได้เปลี่ยน scope หรือ AC)

### ประเด็น 15 (รอบ OSM Data Integration): ต้องเรียก UIUX ต่อ ไม่ข้าม step
- คำตัดสิน: ยืนยันว่ารอบนี้**ต้อง**เรียก UIUX ต่อจาก PM ตามปกติ ไม่ข้าม — มีองค์ประกอบ UI ใหม่จริงที่ต้องออกแบบ 3 จุด: (1) map pin แสดงพิกัด Landmark ในหน้า ProvinceDetailScreen (T67, US-18), (2) ช่องค้นหาสถานที่ใหม่ใน AddEntryScreen พร้อม list ผลลัพธ์ (T70, US-19), (3) ข้อความสถานะ error/timeout/offline/empty ของการค้นหา (T73, US-20) — ทั้ง 3 task ถูกทำเครื่องหมาย **[รอ design-spec]** ไว้แล้วข้างต้น ส่วน task อื่นที่เหลือ (T60, T62, T63, T64, T65, T66, T68, T69, T72, T74) เป็น data/logic layer ล้วนๆ เริ่มขนานกับงาน UIUX ได้ทันทีโดยไม่ต้องรอ
- เหตุผล: ตรงตามขอบเขตหน้าที่ของ PM ที่ห้ามออกแบบ UI เอง และงานทั้ง 3 จุดมีผลต่อประสบการณ์ผู้ใช้โดยตรง (ตำแหน่งจุดบนแผนที่ขนาดเล็กของจังหวัด, การจัดวาง search input คู่กับ dropdown Landmark เดิมไม่ให้สับสน, wording ของ error/empty state) ซึ่งเป็นดุลยพินิจเชิงออกแบบที่ควรมาจาก UIUX ไม่ใช่ PM ตัดสินใจเอง
- ต้องอัป requirements: ไม่ต้อง (เป็นขั้นตอน pipeline ปกติตามที่กำหนดไว้แล้ว ไม่กระทบ scope/AC ของ requirements.md)

### ประเด็น 16 (รอบ Landmark Cards & Media Integration): งาน implement มาก่อน pipeline แล้ว — ข้าม UIUX ย้อนหลัง ไม่ใช่ข้ามโดยตั้งใจตั้งแต่ต้น
- คำตัดสิน: **ไม่เรียก UIUX** สำหรับรอบนี้ แม้มี UI ใหม่จริง 3 จุด (LandmarkCard, in-province search+filter, GlobalSearchBar) เพราะ **โค้ด/UI ถูก implement เสร็จไปแล้วทั้งหมดก่อนที่ pipeline นี้จะรู้จักโจทย์เลย** — การเรียก UIUX ตอนนี้จะเป็นการออกแบบย้อนหลังให้ตรงกับของที่มีอยู่แล้ว ไม่ใช่การออกแบบนำหน้าตามปกติของ pipeline ซึ่งไม่มีประโยชน์เชิงกระบวนการ (ไม่มีอะไรให้ programmer เอาไปสร้างต่อ) จึงข้ามในรอบนี้ ให้ Tester/QA ตรวจสอบ consistency ของ UI ที่มีอยู่จริงแทน (เช่น สี, spacing, accessibility) เทียบกับ theme เดิมของแอป (`src/theme.ts`) ว่ากลมกลืนหรือไม่ ถ้าพบปัญหาเชิง UX ที่ชัดเจนให้ QA ส่งกลับมาที่ PM เพื่อพิจารณาเรียก UIUX จริงในรอบแก้บั๊กถัดไป
- เหตุผล: กฎข้อ 5 ของ pipeline (skip step ที่ไม่จำเป็นสำหรับงานที่ไม่พอดีกับลำดับปกติ) และกฎข้อ 6 (ต้องอัป requirements ให้ตรงกับสิ่งที่เกิดขึ้นจริงเสมอ) ถูกนำมาใช้ร่วมกัน — เนื่องจากนี่คือ retroactive documentation ไม่ใช่ forward planning การบังคับ UIUX design-spec ย้อนหลังจะไม่มีผลต่อโค้ดที่มีอยู่แล้ว เว้นแต่จะพบปัญหาจริงจาก QA
- **ต้องอัป requirements**: ทำไปแล้ว — BA เพิ่ม US-21 ถึง US-24 พร้อมสมมติฐานเพิ่มเติมและ Out of Scope เพิ่มเติมใน `docs/requirements.md` ให้ตรงกับสิ่งที่ implement จริงแล้ว (ดู "สมมติฐานเพิ่มเติม (รอบ Landmark Cards & Media Integration)")
- [x] T86: (เพิ่มหลัง QA รอบ 5) ลบไฟล์ dead code `src/components/LandmarkCardGrid.tsx` — ไม่มีที่ใด import เลย ยืนยันโดย Tester/QA ทั้งคู่แล้ว (อ้างอิง คำตัดสิน PM ข้อ 17) — dependency: ไม่มี
- **จุดเสี่ยงที่ QA ต้องตรวจเป็นพิเศษ**: (1) `LandmarkCardGrid.tsx` เป็น dead code หรือไม่ (ดูหัวข้อ P0 รอบนี้ด้านบน), (2) รายงานของผู้ implement อ้างว่าแก้ `src/types/landmark.ts` แต่ตรวจแล้วไม่ตรง (จริงๆ แก้ที่ `src/data/thailand-landmarks.ts`) — เป็นสัญญาณว่ารายงานอาจมีจุดที่ไม่แม่นยำอื่นซ่อนอยู่ ต้องตรวจสอบอิสระทุกจุดไม่เชื่อคำอ้างเฉยๆ, (3) landmark id จาก Wikipedia (`wiki-{provinceId}-{slug}`) ไม่คงที่ข้ามการ fetch ใหม่ถ้าชื่อบทความเปลี่ยน — เช็คว่า mechanism เช็คอินไม่ throw/crash แม้ landmark id หายไปจากผลลัพธ์รอบถัดไป

### ประเด็น 17 (รอบ Landmark Cards & Media Integration): แก้ QA รอบ 5 — ProvinceMasterBadge (US-9) vs progress indicator ในการ์ด นับ Landmark คนละชุดกัน
- คำตัดสิน: ยืนยัน (formalize) พฤติกรรมที่โค้ดทำอยู่แล้วให้เป็นทางการ — **Province Master badge (US-9) นับความครบเฉพาะ Landmark จาก local seed dataset ที่ curate ไว้ล่วงหน้าเท่านั้น (id คงที่)** ส่วน progress indicator ในการ์ดของหน้าจังหวัด (US-22/US-23) ยังคงนับรวม Wikipedia landmark ด้วยตามเดิม (สะท้อนสิ่งที่ผู้ใช้เห็นบนจอจริงทั้งหมด) — ตัวเลขสองจุดต่างกันได้โดยตั้งใจ ไม่ใช่บั๊ก
- เหตุผล: Wikipedia landmark เป็น runtime/dynamic data ที่ id ไม่คงที่ข้ามการ fetch (ต่างจาก local seed ที่ id ตายตัวถาวร ผูกกับ `CheckinContext` อย่างมั่นคง) ถ้าให้ Province Master นับรวม Wikipedia landmark ด้วย จะทำให้ achievement ที่ควรถาวร/มั่นคง (badge ที่ผู้ใช้ภูมิใจว่า "ครบแล้ว") ผันผวนได้ตามผลการค้นหาที่เปลี่ยนทุกครั้งที่ fetch ใหม่ (เช่น Wikipedia แก้ชื่อบทความ หรือเพิ่ม/ลดหมวดหมู่) ซึ่งขัดกับ "ความรู้สึกสำเร็จที่ยั่งยืน" ที่ US-9 ตั้งใจสื่อ — การแยกสองตัวเลขนี้ยังต้นทุนต่ำที่สุดเพราะ**โค้ดปัจจุบันทำแบบนี้อยู่แล้วโดยไม่ได้ตั้งใจ** (ตรวจสอบแล้วจาก Tester) จึงแค่ยืนยัน ไม่ต้องแก้โค้ดเพิ่ม
- **ต้องอัป requirements**: ทำแล้ว — เพิ่ม AC ใหม่ใน US-9 ของ `docs/requirements.md` ระบุชัดว่า Province Master นับเฉพาะ local seed dataset ไม่รวม Wikipedia landmark เพื่อปิด scope ambiguity ที่ QA รอบ 5 พบ
- หมายเหตุ UX ที่ยังไม่ใช่ blocking: ตัวเลขสองจุดที่ต่างกันในหน้าเดียวอาจทำให้ผู้ใช้สับสนได้ในอนาคต (เช่น "ทำไมการ์ดบอกครบแล้วแต่ badge ไม่ขึ้น") — ถ้ามี feedback จริงจากผู้ใช้ ให้พิจารณาเรียก UIUX ออกแบบ label ที่ชัดเจนกว่านี้แยกสองแนวคิด (เช่น "สำรวจแล้ว X/Y" ของการ์ด vs "Province Master ครบ Top Landmark" ของ badge) ในรอบถัดไป ไม่ใช่ scope ของรอบนี้

### ประเด็น 18 (US-25 Bug Fix): ไม่เรียก UIUX เพิ่ม — ใช้ pattern error+retry ที่มีอยู่แล้วในระบบ
- คำตัดสิน: **ไม่เรียก UIUX** สำหรับ error state ของ US-25 — ให้ programmer ออกแบบ inline เองตาม pattern ที่มีอยู่แล้วจริงในระบบคือ `src/components/SearchPlaceField.tsx` (มี prop `onRetry` ผูกกับ `useNominatimSearch`/`AddEntryScreen` จากรอบ OSM Data Integration US-20 ที่ผ่าน design-spec และ QA มาแล้ว) ซึ่งมีทั้งข้อความ error สื่อความหมายและปุ่มลองใหม่เป็นแบบแผนอยู่แล้ว ไม่ใช่การออกแบบ UI ใหม่ตั้งแต่ศูนย์ — T87–T89 จึงไม่ติดป้าย **[รอ design-spec]**
- เหตุผล: AC ข้อ 3 ของ US-25 เขียนไว้ชัดว่า "ไม่ระบุรูปแบบ UI ที่แน่นอน" เพียงต้องมีทางลองใหม่ได้ — ขอบเขตของงานนี้คือ error message + retry action ธรรมดา ซึ่งมี precedent ที่ผ่านการออกแบบและ QA แล้วในแอปเดียวกัน (`SearchPlaceField.tsx`, `AddEntryScreen.tsx` errorText/`COLORS.danger`) ต่างจากรอบ OSM เดิม (ดูประเด็น 15) ที่มีองค์ประกอบ UI ใหม่จริง 3 จุดที่ไม่เคยมี pattern มาก่อนในแอป (map pin, search input ใหม่, wording ใหม่ทั้งชุด) การบังคับเรียก UIUX รอบนี้จะเพิ่มเวลาโดยไม่ได้ประโยชน์เชิง UX ที่ชัดเจน เพราะ consistency ที่ดีที่สุดของ error state เล็กๆ แบบนี้คือการ reuse pattern เดิมตรงๆ ไม่ใช่ออกแบบใหม่ที่เสี่ยงลดความสม่ำเสมอของแอป — programmer ต้องอ้างอิง `SearchPlaceField.tsx` และ `src/theme.ts` เป็นฐาน ไม่ใช่คิดสี/wording ใหม่เอง
- ต้องอัป requirements: ไม่ต้อง (ไม่เปลี่ยน scope/AC ใดๆ ของ US-25 ตามที่ BA เขียนไว้ — เป็นการตัดสินใจ pipeline step เท่านั้น)

### ประเด็น 19 (รอบ 7: Bottom Tab Navigation & ข่าว RSS): ลำดับ fallback RSS source ที่ต้องลองถ้า `tatnews.org/feed/` ใช้ไม่ได้ + ค่า TTL cache
- คำตัดสิน: กำหนดให้ T90 (verify endpoint) เป็น task แรกสุดที่ต้องทำก่อนเขียนโค้ดฝั่งข่าวทั้งหมด และกำหนดลำดับ fallback ที่ programmer ต้องลองตามลำดับถ้าตัวเลือกแรกยืนยันแล้วว่าใช้ไม่ได้จริง (ไม่ active หรือไม่ใช่ valid RSS/Atom XML):
  1. `https://www.tatnews.org/feed/` (ตัวเลือกแรกจาก BA — ลองก่อนเสมอ)
  2. RSS หมวดท่องเที่ยวของ Bangkok Post (เช่น `bangkokpost.com/rss/data/travel.xml` หรือ endpoint ปัจจุบันที่ใกล้เคียงที่สุด ณ เวลา implement — สำนักข่าวใหญ่ที่เปิด RSS สาธารณะมายาวนาน)
  3. RSS หมวดใกล้เคียง (ท่องเที่ยว/lifestyle) ของ The Nation Thailand
  4. ถ้าทั้ง 3 ใช้ไม่ได้จริง ให้ programmer เลือก RSS สาธารณะอื่นของสำนักข่าว/หน่วยงานท่องเที่ยวไทยที่มีหมวดข่าวท่องเที่ยวชัดเจนเอง โดยต้อง log เหตุผลและ URL ที่เลือกไว้ใน `docs/dev-notes.md` (ตามที่ BA กำหนดไว้แล้วในสมมติฐาน)
  นอกจากนี้กำหนดค่า TTL ของ cache ข่าว (T100) เป็น **45 นาที** (ค่ากึ่งกลางของช่วง 30–60 นาทีที่ BA เสนอไว้เป็นตัวเลือกให้ทีม dev กำหนดเอง)
- เหตุผล: PM ไม่มีเครื่องมือ browse อินเทอร์เน็ตจริงในบทบาทนี้เพื่อยืนยัน URL ทั้งหมด ณ ตอนวางแผน จึงกำหนดลำดับ fallback ที่สมเหตุผล (สำนักข่าวใหญ่ที่เป็นที่รู้จักว่ามักเปิด RSS สาธารณะ) ให้ programmer ลองตามลำดับแทนที่จะปล่อยให้เลือกเองแบบไม่มีทิศทางซึ่งอาจเสียเวลาค้นหาซ้ำซ้อน ส่วนค่า TTL เลือกค่ากึ่งกลางเพราะสมดุลระหว่างความสดใหม่ของข่าวกับการลด request ซ้ำ โดยไม่กระทบ pull-to-refresh ที่ผู้ใช้บังคับดึงใหม่ได้เสมอตาม AC เดิม
- ต้องอัป requirements: ไม่ต้อง (BA เขียนไว้แล้วว่าต้อง verify และหาแหล่งสำรองเอง พร้อม log ว่าเปลี่ยนไปใช้แหล่งใด และ TTL เป็นค่าที่ทีม dev กำหนดเองตามสมมติฐานเดิม — เป็นการกำหนดรายละเอียด implementation เท่านั้น ไม่เปลี่ยน scope/AC)

### ประเด็น 20 (รอบ 7): Dependency ที่ยังไม่มีในโปรเจกต์จริง — ต้องติดตั้งใหม่ (ต่างจากรอบ Supabase ที่มีอยู่แล้ว)
- คำตัดสิน: ตรวจสอบ `package.json` แล้วยืนยันว่า `@react-navigation/bottom-tabs` และ `expo-web-browser` **ไม่มี**อยู่ในโปรเจกต์เลย (ต่างจากคำตัดสิน PM ข้อ 8 ของรอบ Supabase ที่ dependency ที่ต้องการมีอยู่แล้วทั้งหมด) จึงต้องเพิ่ม task ติดตั้งจริง (T91) ให้ทำก่อน T92 (navigation wiring) และ T105 (in-app browser wiring)
- เหตุผล: ป้องกัน programmer เข้าใจผิดว่า dependency พร้อมใช้แล้วเหมือนรอบก่อนหน้า ต้องระบุชัดเจนเพราะเป็นจุดที่ต่างจาก pattern เดิมของโปรเจกต์นี้
- ต้องอัป requirements: ไม่ต้อง (รายละเอียด implementation ล้วนๆ)

### ประเด็น 21 (รอบ 7): วิธี parse RSS/XML — ใช้ custom regex/string-based parser แทนการพึ่งพา XML library ภายนอก
- คำตัดสิน: กำหนดให้เขียน parser เอง (T98) ด้วยวิธี regex/string extraction สำหรับ field ที่ต้องการ (title, description, enclosure/media:content, link, pubDate) แทนที่จะติดตั้ง XML/DOM parsing library ภายนอกเพิ่ม (เช่น xml2js, rss-parser, fast-xml-parser) — อนุญาตให้ programmer เปลี่ยนไปใช้ library ที่ compatible กับ React Native ได้ภายหลังถ้าพิสูจน์แล้วว่า custom parser จัดการ edge case ของ endpoint ที่เลือกจริงไม่ไหว แต่ให้เริ่มจาก custom parser ก่อน
- เหตุผล: (1) React Native ไม่มี `DOMParser` ในตัว ทำให้ library XML/RSS ยอดนิยมหลายตัวที่ออกแบบมาสำหรับ browser/Node DOM ใช้ไม่ได้ตรงๆ ต้องมี polyfill เพิ่มซึ่งเพิ่มความเสี่ยง compatibility ที่ไม่จำเป็นและเป็น dependency เพิ่มอีกตัวนอกเหนือจากที่ตัดสินใจแล้วในประเด็น 20 (2) field ที่ต้องการ parse มีจำนวนจำกัดและมีโครงสร้างที่คาดเดาได้ตามมาตรฐาน RSS 2.0 การเขียน parser เองด้วย regex ที่ครอบคลุม tag เหล่านี้ทำได้ในเวลาที่สมเหตุผลและควบคุม edge case ได้เต็มที่ตาม AC ของ US-29/US-32 (3) ลดจำนวน dependency ใหม่ที่ต้องติดตั้งเพิ่ม
- ต้องอัป requirements: ไม่ต้อง (เป็นรายละเอียด implementation — requirements.md ระบุแค่ field ที่ต้อง parse ไม่ได้ระบุวิธี/library ที่ต้องใช้)

### ประเด็น 22 (รอบ 7): ต้องเรียก UIUX ต่อสำหรับ NewsScreen UI — ไม่ข้าม (ต่างจากรอบ Landmark Cards ที่เป็น retroactive)
- คำตัดสิน: รอบนี้ต้องเรียก UIUX ตามปกติของ pipeline ไม่ข้าม — NewsScreen, news item card, empty/error state ของหน้าข่าว และการปรับ header icon ให้ทำงานถูกต้องใน 2 แท็บ เป็น UI ใหม่ทั้งหมดที่ยังไม่เคย implement มาก่อนในแอปนี้ — task ที่ติดป้าย **[รอ design-spec]** คือ T94 (ไอคอน tab bar) และ T101-T104 (NewsScreen UI ทั้งหมด) ส่วน T90-T93, T95-T100, T105-T109 เป็น non-UI (verification/dependency/navigation wiring/data/logic layer) เริ่มขนานกับ UIUX ได้ทันที
- เหตุผล: ตรงตามขอบเขตหน้าที่ของ PM ที่ห้ามออกแบบ UI เอง และงานนี้เป็น forward planning ปกติของ pipeline (BA เพิ่งเขียน requirements ก่อนเริ่ม implement จริง ไม่ใช่ retroactive documentation แบบรอบ Landmark Cards & Media Integration ที่โค้ดเสร็จไปก่อนแล้ว) จึงไม่มีเหตุผลพิเศษให้ข้าม UIUX เหมือนรอบนั้น
- ต้องอัป requirements: ไม่ต้อง (เป็นขั้นตอน pipeline ปกติ ไม่กระทบ scope/AC ของ requirements.md)

### ประเด็น 23 (รอบ 7): รูปแบบ UI แจ้ง "เปิดข่าวไม่ได้ / ไม่มีเน็ต" (design-spec Issue E, US-31/US-32) + ยืนยัน Issue F, G ที่ non-blocking
- คำตัดสิน:
  - **Issue E**: เลือกทางเลือก A (transient toast auto-dismiss) เป็นฐานตามที่ UIUX ออกแบบไว้ เพื่อคงความสอดคล้องกับ toast pattern ที่มีอยู่แล้วในแอป (เช่น toast "ผูกอีเมลสำเร็จ"/"บันทึกไม่สำเร็จ ลองใหม่อีกครั้ง") และไม่เพิ่ม state ค้างในหน้าจอที่ต้อง manage เพิ่ม — แต่ปรับ **duration จาก ~2.5 วินาทีที่เสนอไว้เดิม เป็น ~4 วินาที** สำหรับทั้งสอง state (`link-invalid` และ `offline`) เพื่อลดความเสี่ยงที่ผู้ใช้จะพลาดข้อความ โดยเฉพาะกรณี offline ที่ผู้ใช้อาจไม่ทราบสาเหตุทันทีตามที่ UIUX กังวลไว้ — ปรับ T106/T107 ให้ใช้ค่า duration ใหม่นี้แทนค่าเดิมใน design-spec
  - **Issue F**: ยอมรับแนวทางที่ UIUX เสนอ — ไม่บังคับซ่อน/ไม่ซ่อน Bottom Tab Bar ตอนอยู่ใน AddEntryScreen ปล่อยเป็นดุลยพินิจ programmer ตาม native default behavior ของ `@react-navigation/bottom-tabs` ที่ tab bar ยังแสดงอยู่ระหว่าง stack screen ที่ push ทับตามปกติ
  - **Issue G**: ยอมรับแนวทางที่ UIUX เสนอ — ใช้ in-app icon placeholder (ไม่ hotlink ภาพ stock ภายนอกที่ไม่ได้ curate) สำหรับ News Card Placeholder Cover ตามที่ design-spec ระบุไว้แล้ว ไม่ต้อง bundle stock photo เพิ่มเข้า asset ของแอป
- เหตุผล:
  - (Issue E) AC ของ US-31 ข้อ 3 และ 4 ระบุเพียงว่าต้อง "แสดงข้อความแจ้ง" โดยไม่ได้กำหนดว่าต้องเป็น persistent UI — การเปลี่ยนไป Option B (persistent inline banner) จะเพิ่ม state ใหม่ที่ต้อง manage เพิ่มเติม (เช่น ต้อง clear เมื่อไหร่, ซ้อนกับ Cache Indicator ที่เป็น `ListHeaderComponent` เดิมของ NewsScreen หรือไม่) โดยยังไม่มีหลักฐานว่าจำเป็นจริง เพราะ edge case นี้ (ลิงก์เสีย/ไม่มีเน็ตตอนกดข่าว) เกิดไม่บ่อยและ recover ได้ง่ายมาก (ผู้ใช้แค่ลองกดข่าวอื่น หรือกดข่าวเดิมซ้ำอีกครั้งเมื่อมีเน็ต) ไม่ใช่ error ระดับที่ต้อง block การอ่านข่าวอื่นหรือค้างจอไว้จนกว่าจะกดปิดเอง การขยาย duration จาก 2.5s เป็น 4s คือทางแก้ที่ต้นทุนต่ำที่สุดที่ตรงประเด็นความเสี่ยงหลักที่ UIUX ยกมา (ผู้ใช้ "มองไม่ทัน") โดยไม่ต้องเพิ่ม component/state ใหม่เลย
  - (Issue F, G) ทั้งสองข้อ UIUX ระบุเองว่า non-blocking และเหตุผลที่ให้ไว้สมเหตุผลดีอยู่แล้ว — Issue F อิงตาม native library behavior ที่ไม่กระทบ UX หลักไม่ว่าจะเลือกทางใด ส่วน Issue G สอดคล้องกับแนวทางที่ทีมนี้ยึดถือมาตลอดเรื่อง asset reliability (เทียบได้กับเหตุผลเดียวกับที่ทีมเลือก hotlink `FALLBACK_LANDMARK_IMAGE` จาก Wikimedia ได้เพราะ curate แล้ว แต่ไม่ hotlink โดเมนข่าวทั่วไปที่ไม่ได้ curate) จึงไม่มีเหตุผลให้เปลี่ยนจากที่ UIUX เสนอ
- ผลกระทบต่อ design-spec: **ไม่กระทบอย่างมีนัยสำคัญ** — เลือก Option A ไม่ใช่ Option B จึงไม่มีการเพิ่ม/เปลี่ยน component ใหม่ใดๆ ที่ UIUX ต้องออกแบบใหม่ มีเพียงตัวเลข duration parameter ของ "News Open-Failure Toast (T106, T107)" ที่เปลี่ยนจาก "~2.5 วินาที" เป็น "~4 วินาที" ซึ่งเป็นรายละเอียด implementation ระดับตัวเลขเดียว ไม่ใช่การเปลี่ยนโครงสร้างหรือ visual design — **ไม่ต้องให้ UIUX กลับไปอัปเดต design-spec.md** ให้ programmer (T106/T107) ยึดค่า 4 วินาทีตามคำตัดสินนี้แทนตัวเลขเดิมที่เขียนไว้ในเอกสารได้เลย
- ต้องอัป requirements: ไม่ต้อง (เป็นรายละเอียด implementation/UI form ไม่ใช่ acceptance criteria ใหม่ — ไม่กระทบ scope ของ requirements.md เดิม)

### ประเด็น 24 (รอบ 14): เลือกทางเลือกทางเทคนิคของ US-33 (รูปภาพข่าวไม่แสดง) — ทางเลือก (ข) fetch og:image แบบ lazy เป็นหลัก พร้อม contingency ไปทางเลือก (ก)
- คำตัดสิน: เลือก **ทางเลือก (ข)** (fetch Open Graph image ต่อบทความแบบ lazy) เป็นแนวทางหลักที่ต้อง implement ก่อน — สร้าง T113 (verify og:image availability + response time จริงก่อนลงมือเขียนโค้ดเต็มรูปแบบ) → T114 (โมดูล fetch แบบ lazy + timeout + cache + isolate ต่อรายการ) → T115 (wire เข้า NewsCard) → T117 (test + รายงานตัวเลขจริงตาม AC) กำหนด **T116 เป็น contingency task** ที่ทำเฉพาะกรณี T113 พิสูจน์ว่า (ข) ทำไม่ได้จริง (เช่น og:image หายากผิดคาด/timeout สูงเกินไปเป็นส่วนใหญ่) จึงค่อยสลับไปทางเลือก (ก) โดยใช้ลำดับ fallback source ที่มีอยู่แล้วจากคำตัดสิน PM ข้อ 19 เป็นฐาน (ต้อง verify ว่ามีรูปแนบจริงในตัว feed ก่อนสลับ ไม่ใช่สลับไปเดาเอง)
- เหตุผล: (1) ทางเลือก (ก) ต้องหาแหล่งข่าวใหม่ที่ verify แล้วว่ามีรูปแนบจริง ซึ่งเป็นความไม่แน่นอนซ้ำซ้อนกับที่เพิ่งพบว่าลำดับ fallback เดิม (คำตัดสิน PM ข้อ 19) ก็ยังไม่เคย verify เรื่องรูปภาพมาก่อน (verify แค่ว่า endpoint active และ parse ได้) — ถ้าเลือก (ก) แล้วพบว่าแหล่งสำรองก็ไม่มีรูปเช่นกัน จะต้องเปลี่ยนแหล่งซ้ำอีกและต้องทดสอบ parser ใหม่ทั้งชุด (title/summary/link/pubDate) เสี่ยง regression กับ US-29/US-32 ที่ผ่าน QA ไปแล้ว (2) ทางเลือก (ข) ไม่ต้องแตะ RSS source/parser ที่ทดสอบผ่านแล้ว (T97-T100, T111) เลย ลดความเสี่ยง regression และรับประกันผลลัพธ์ได้มากกว่าในทางทฤษฎี (บทความข่าวทั่วไปส่วนใหญ่มี og:image สำหรับ social sharing) (3) ความเสี่ยงหลักของ (ข) คือ performance/rate-limit ซึ่งบรรเทาได้ด้วย lazy fetch ต่อรายการที่แสดงผลจริง + timeout สั้น + cache ตาม TTL เดิม ซึ่งเป็นรูปแบบที่ทีมนี้ทำอยู่แล้ว (เทียบกับ opportunistic network check ของ `SyncContext.tsx`/T73) ไม่ใช่ pattern ใหม่ที่ไม่เคยพิสูจน์ในโปรเจกต์นี้ (4) ยังคง T113 เป็นเงื่อนไข verify ก่อนเสมอ และเปิดทาง T116 ไว้เป็น fallback ที่ทำได้จริงถ้า (ข) ล้มเหลว จึงไม่ปิดประตูทางเลือก (ก) ทิ้งไปเฉยๆ ตามที่ requirements.md ต้องการให้ PM ตัดสินใจโดยพิจารณา trade-off จริง ไม่ใช่เดาเอาเอง
- ต้องอัป requirements: ไม่ต้อง (requirements.md เขียน AC ของ US-33 ให้ครอบคลุมทั้งสองทางเลือกไว้แล้วโดยตั้งใจ และระบุชัดว่าการเลือกทางใดทางหนึ่งเป็นหน้าที่ของ PM/Programmer ไม่ใช่ acceptance criteria ใหม่ — คำตัดสินนี้เป็นการเลือกแนวทาง implementation ภายในกรอบ AC เดิมทั้งหมด)

### ประเด็น 25 (รอบ 14): Home 3D map (76 tiles) ไม่มี entrance animation ตาม AC ขั้นต่ำของ US-34 vs complaint ของผู้ใช้ว่า "ทั้งแอปเหมือนเดิมตั้งแต่วันแรก"
- คำตัดสิน: เลือกแนวทางผสมที่ปลอดภัยของทางเลือก B (ตามที่ UIUX เสนอ) — ให้ HomeScreen 3D map grid container ของ 76 tiles ทั้งก้อน (single-opacity animation ของ container เดียว **ไม่ animate ทีละ tile** และไม่ animate ตำแหน่ง/scale ของ tile ใดๆ) เล่น fade-in ครั้งเดียวตอนเข้าหน้า Home (~250–300ms) ควบคู่กับ header/legend (จำนวน unlocked, progress bar) fade-in แยกต่างหากตามทางเลือก A ที่ UIUX เสนอไว้เดิม — รวมสองส่วนนี้เป็นจุดที่ 4 เพิ่มจาก 3 จุดขั้นต่ำของ AC (Landmark list, News list, Stats timeline) และปรับ T120 ให้ครอบคลุมจุดนี้ด้วย
  - **Safety net (บังคับ ไม่ต้องขอ PM ตัดสินซ้ำ)**: programmer ต้องทดสอบบนอุปกรณ์จริงอย่างน้อย 1 เครื่องระดับกลาง/ล่างก่อนถือว่า T120 เสร็จสมบูรณ์ ถ้าพบ jank/หน่วงที่สังเกตได้ระหว่างเล่น fade-in ของ grid container ไม่ว่าจากสาเหตุใด ให้ fallback ทันทีเป็นทางเลือก A ล้วน (ตัด fade-in ของ grid container ออก เหลือแค่ header/legend fade-in) โดยไม่ต้องกลับมาขอ PM ตัดสินใจซ้ำ — บันทึกผลการทดสอบและเหตุผล fallback (ถ้ามี) ไว้ใน `docs/dev-notes.md` เพื่อให้ QA ตรวจสอบย้อนหลังได้
- เหตุผล: (1) ผู้ใช้ร้องเรียนตรงประเด็นว่า "ทั้งแอปเหมือนเดิมตั้งแต่วันแรก" และ HomeScreen คือหน้าแรกสุดที่เห็นทุกครั้งที่เปิดแอป — ถ้าเลือกทางเลือก A ล้วน (ไม่มี motion ใดๆ บน tile grid เลย) ความรู้สึก "หน้าแรกเหมือนเดิม" จะยังคงอยู่ตรงจุดที่สำคัญที่สุดของแอป แม้หน้าอื่นทั้งหมดจะมี motion ชัดเจนแล้วก็ตาม จึงไม่ตอบโจทย์ complaint นี้โดยตรงเท่าที่ควร (2) ทางเลือก B ตามที่ UIUX เสนอมีความเสี่ยง performance ต่ำกว่าที่ AC เดิมกังวลไว้มาก เพราะเป็นการ animate ค่าเดียว (opacity ของ parent container) ไม่ใช่ 76 animated value พร้อมกันทีละ tile — ในทางทฤษฎีไม่กระทบ P0 เดิมของ US-1 (ต้องแสดงแผนที่ 76 จังหวัดได้ปกติ ไม่หน่วง/ค้าง) แต่เนื่องจากยังไม่เคยพิสูจน์จริงบนอุปกรณ์จริงของโปรเจกต์นี้มาก่อน จึงยังต้องมี on-device verification ก่อนถือว่าปลอดภัยจริง ไม่ใช่รับข้อเสนอไปตรงๆ โดยไม่ตรวจสอบ (3) กำหนด safety net ให้ fallback อัตโนมัติกลับไปทางเลือก A ได้ทันทีโดยไม่ต้องขอ PM ตัดสินซ้ำ เพื่อไม่ให้ pipeline ติดขัดกลางทางถ้าพบปัญหาจริงหน้างาน สอดคล้องกับหลักการเดียวกับ fallback ที่วางไว้ล่วงหน้าใน T61 (รอบ OSM Data Integration) (4) AC ของ US-34 เขียนไว้ว่ารายการหลักที่ต้องมี entrance animation คือ "อย่างน้อย" 3 จุดนี้ ไม่ใช่ "เฉพาะ" 3 จุดนี้เท่านั้น การเพิ่มจุดที่ 4 แบบมี safety net จึงยังอยู่ในกรอบ AC เดิม ไม่ใช่การขยาย scope ใหม่ที่ BA ไม่ได้ตั้งใจ
- ต้องอัป requirements: ไม่ต้อง (อยู่ในกรอบ AC เดิมของ US-34 ที่ระบุ list ขั้นต่ำไว้แล้วด้วยคำว่า "อย่างน้อย" — การเพิ่มจุดที่ 4 แบบมี safety net fallback เป็นรายละเอียด implementation/design ที่ตัดสินใจได้ภายในขอบเขต PM ร่วมกับ UIUX ที่เสนอมา ไม่กระทบ scope หรือ acceptance criteria ที่ BA เขียนไว้)

## รอบ 15: Color Palette & Layout Redesign — เฟส 1 (US-35 Decision Gate + US-36 HomeScreen)

> เฟส 2 (US-37 หน้าจอที่เหลือ + US-38 Visual QA เต็มระบบ) เป็น backlog รอคิวถัดไป ยังไม่แตก task ละเอียดในรอบนี้ตามการตัดสินใจเรื่องลำดับงานของผู้ใช้

### P0 (ต้องมี — เฟส 1)
- [ ] T124: **[ทำก่อนสุด — ส่งต่อ orchestrator ให้เรียก UIUX ก่อน]** UIUX เสนอ 2–3 ตัวเลือกทิศทาง color palette ใหม่ทั้งระบบ (แต่ละตัวเลือกต้องมี: ชุด hex ตัวอย่างของ `COLORS` หลัก, mood/คำอธิบายสั้นๆ, ตัวอย่าง mockup หรือคำอธิบายภาพของ 1-2 หน้าจอ) ต่อท้ายใน `docs/design-spec.md` เพื่อให้ orchestrator นำไปให้ผู้ใช้เลือกยืนยันก่อนเริ่มลงมือแก้โค้ดใดๆ (อ้างอิง US-35 AC1, Decision Gate ข้อ 1) — dependency: ไม่มี
- [ ] T125: **[Decision Gate — ไม่ใช่ task โค้ด]** รอผู้ใช้ยืนยันเลือก 1 ใน 2–3 ตัวเลือกจาก T124 อย่างเป็นทางการ ก่อนปลดล็อก task ถัดไปทั้งหมดในรอบนี้ — dependency: T124
- [x] T126: ปรับปรุงค่าใน `src/theme.ts` (`COLORS`, และ `CATEGORY_COLORS`/`SHADOWS` เท่าที่จำเป็นตามโทนที่เลือก) ตามตัวเลือกที่ผู้ใช้ยืนยันใน T125 เป็นจุดเดียว ห้าม hardcode สีใหม่กระจายไฟล์อื่น พร้อมตรวจสอบคู่สีข้อความสำคัญผ่านเกณฑ์ WCAG AA (contrast ≥ 4.5:1) และคงความหมายเชิงสถานะเดิม (เขียว/โทนหลัก=unlocked, เทา=locked, เหลือง/ทอง=warning/Province Master, แดง=danger) (อ้างอิง US-35 AC2,3,4,5) — dependency: T125
- [x] T127: Redesign layout ของ `HomeScreen.tsx` (ตำแหน่ง/spacing ของ header, legend, แถบค้นหา ตามมาตรฐาน 8pt grid ของ `SPACING`) โดยไม่เปลี่ยน information architecture หรือฟังก์ชันเดิม (อ้างอิง US-36 AC1,3,4) — dependency: T126
- [x] T128: ปรับสี/แสงเงา/gradient ของ `Map3D.tsx` และ `ProvinceTile3D.tsx` ให้ใช้ token ใหม่จาก T126 — **ขอบเขตจำกัดเฉพาะสี/material/shadow บนโครงสร้าง geometry เดิม ห้ามเปลี่ยนรูปทรงบล็อก/มุมกล้อง/จำนวน tile** ตามคำตัดสิน PM ข้อ 26 ด้านล่าง (อ้างอิง US-36 AC1,3) — dependency: T126
- [x] T129: ปรับ `HeaderProgress.tsx`, `Legend.tsx`, `GlobalSearchBar.tsx` (เฉพาะจุดที่แสดงอยู่ในหน้า Home) ให้ใช้ `COLORS`/`SPACING`/`RADIUS`/`SHADOWS` ชุดใหม่ครบทุกจุดที่เกี่ยวกับสี ไม่มี hex เดิมหลงเหลือ (อ้างอิง US-36 AC1) — dependency: T126
- [x] T130: Regression guard — ยืนยันว่าฟังก์ชันเดิมของหน้า Home ทั้งหมด (จำนวนจังหวัดปลดล็อก, แตะจังหวัดเข้า ProvinceDetail, ผลค้นหา+นำทาง, Province Master ตาม US-14) และ animation จาก US-34 (T119–T121) ยังทำงาน/มีพฤติกรรมเหมือนเดิมทุกประการหลังปรับ T127–T129 ไม่มี regression ต่อ US-1, US-2, US-3, US-14, US-24, US-26 (อ้างอิง US-36 AC4,5) — dependency: T127, T128, T129

### P1 (ควรมี — เฟส 1)
- [x] T131: เขียน/ปรับ automated test หรือ code-review checklist ยืนยันว่าไม่มีค่าสี hex เดิม (hardcode) หลงเหลือใน `HomeScreen.tsx`, `Map3D.tsx`, `ProvinceTile3D.tsx`, `HeaderProgress.tsx`, `Legend.tsx`, `GlobalSearchBar.tsx` ที่ไม่ได้อ้างอิงจาก `theme.ts` (อ้างอิง US-36 AC1) — dependency: T130

## รอบ 18: Color Palette & Layout Redesign — เฟส 2 (US-37 หน้าจอที่เหลือ + US-38 Visual QA)

### P0 (ต้องมี — เฟส 2)
- [x] T132: **[ส่งต่อ orchestrator ให้เรียก UIUX ก่อน]** UIUX ออกแบบ layout/spacing spec ของ `ProvinceDetailScreen.tsx` และ `AddEntryScreen.tsx` (2 หน้าที่ยังไม่เคย redesign อย่างเป็นทางการมาก่อน) ตามมาตรฐาน 8pt grid ของ `SPACING`/`RADIUS`/`SHADOWS` ชุดเดิมที่ใช้กับ HomeScreen v4 (T127) โดยไม่เปลี่ยน information architecture/ฟังก์ชันเดิม (อ้างอิง US-37 AC3) — dependency: ไม่มี (palette ตัดสินใจแล้วจาก T126)
- [x] T133: Programmer ปรับ `ProvinceDetailScreen.tsx` ตาม spec จาก T132 + สลับ `COLORS` token ใหม่ให้ครบ ไม่กระทบ functional behavior ของ US-8/US-9/US-22/US-23 (อ้างอิง US-37 AC2,3,4) — dependency: T132
- [x] T134: Programmer ปรับ `AddEntryScreen.tsx` ตาม spec จาก T132 + สลับ `COLORS` token ใหม่ให้ครบ ไม่กระทบ form validation ของ US-4 (อ้างอิง US-37 AC2,3,4) — dependency: T132
- [x] T135: Programmer สลับ `COLORS`/`SPACING`/`RADIUS`/`SHADOWS` token ชุดใหม่ใน `LandmarkCard.tsx` ให้ครบ ไม่มี hex เดิม (#1D9E75/#0F6E56) หลงเหลือ (อ้างอิง US-37 AC1,2,4) — dependency: ไม่มี (T126 เสร็จแล้ว)
- [x] T136: Programmer สลับ token ชุดใหม่ใน `StatsScreen.tsx` ให้ครบ ไม่กระทบการแสดงสถิติของ US-6 (อ้างอิง US-37 AC1,2,4) — dependency: ไม่มี
- [x] T137: Programmer สลับ token ชุดใหม่ใน `SettingsScreen.tsx` ให้ครบ (อ้างอิง US-37 AC1,2,4) — dependency: ไม่มี
- [x] T138: Programmer สลับ token ชุดใหม่ใน `NewsScreen.tsx` (รวมการ์ดข่าว) ให้ครบ ไม่กระทบการแสดงข่าวของ US-29 ถึง US-32 (อ้างอิง US-37 AC1,2,4) — dependency: ไม่มี
- [x] T139: Regression guard — ยืนยันว่า functional behavior เดิมทั้งหมดของ 6 หน้าจอที่แก้ (T133-T138) และ animation จาก US-34 ยังทำงานเหมือนเดิมทุกประการ ไม่มี regression ต่อ US-4, US-6, US-8, US-9, US-22, US-23, US-29 ถึง US-32 (อ้างอิง US-37 AC4) — dependency: T133, T134, T135, T136, T137, T138

### P1 (ควรมี — เฟส 2)
- [x] T140: เขียน/ปรับ automated test หรือ code-review checklist ยืนยันว่าไม่มีค่าสี hex เดิมหลงเหลือใน `LandmarkCard.tsx`, `ProvinceDetailScreen.tsx`, `AddEntryScreen.tsx`, `StatsScreen.tsx`, `SettingsScreen.tsx`, `NewsScreen.tsx` ที่ไม่ได้อ้างอิงจาก `theme.ts` (ต่อยอด T131) (อ้างอิง US-37 AC1) — dependency: T139
- [ ] T141: Tester/QA เตรียม checklist + screenshot ก่อน/หลังครบทุกหน้าจอหลัก (HomeScreen, ProvinceDetailScreen, AddEntryScreen, StatsScreen, SettingsScreen, NewsScreen) (อ้างอิง US-38 AC1) — dependency: T139 — **ยังไม่ทำ: ไม่มี screenshot จริง (ไม่มี runtime/อุปกรณ์ในสภาพแวดล้อมนี้) เป็น coverage gap เดียวกับ T30/US-31 AC2 เดิม**
- [x] T142: ตรวจสอบไม่มี hex hardcode ใหม่เพิ่มขึ้นในไฟล์ที่แก้รอบนี้ (ต่อยอด T140) (อ้างอิง US-38 AC2) — dependency: T140 — ยืนยันด้วย grep อิสระ + `noHardcodedHexPhase2.test.ts` (T140) ผ่านครบ 6/6 ไฟล์ในขอบเขตที่มอบหมาย (ดูข้อยกเว้น `NewsErrorState.tsx` ใน dev-notes.md ที่ไม่อยู่ในสโคป T138)
- [x] T143: รัน automated test suite เต็ม + `tsc --noEmit` ยืนยัน regression-free ต่อ US-34 และฟีเจอร์เดิมทั้งหมด (อ้างอิง US-38 AC3) — dependency: T139 — ยืนยันอิสระ: `tsc --noEmit` สะอาด, `npx jest` 84/84 suites, 487 passed + 1 skip, 0 failed
- [ ] T144: **[Manual step — ไม่ใช่ automated, ต้องรอผู้ใช้ยืนยันเอง]** ผู้ใช้เปิดแอปจริงอย่างน้อย 1 รอบ ยืนยันความรู้สึกว่า "แอปเปลี่ยนไปจริง ดู advance ขึ้น" ก่อนปิดงานรอบนี้ (อ้างอิง US-38 AC4) — dependency: T141, T142, T143 — **รอผู้ใช้ยืนยันเอง ไม่มีใครแทนได้ (คำตัดสิน PM ประเด็น 28)**

## คำตัดสิน PM (ต่อ)

### ประเด็น 27 (รอบ 18): ทำไมไม่ต้องมี Decision Gate ใหม่สำหรับเฟส 2 + ทำไมเรียก UIUX แค่ 2 หน้า ไม่ใช่ทั้ง 6 หน้า
- คำตัดสิน: ไม่ต้องมี decision gate ใหม่ (palette ตัดสินใจปิดไปแล้วที่ T125 "Deep Jade") เรียก UIUX เฉพาะ `ProvinceDetailScreen`/`AddEntryScreen` (T132) ส่วนอีก 4 หน้า (`LandmarkCard`, `StatsScreen`, `SettingsScreen`, `NewsScreen`) ให้ programmer สลับ token ตรงได้เลยไม่ต้องรอ UIUX
- เหตุผล: requirements.md US-37 AC3 ระบุชัดว่ามีแค่ `ProvinceDetailScreen`/`AddEntryScreen` ที่ "ยังไม่เคยผ่านการ redesign อย่างเป็นทางการมาก่อน" ต้องมี layout/spacing spec ใหม่ ส่วนอีก 4 หน้าผ่าน Bento-card redesign รอบ v2-v4 มาแล้ว (มี pattern/token ของ `SPACING`/`RADIUS`/`SHADOWS` อยู่แล้ว) ตรงกับ AC532 (ทิศทาง A: ปรับปรุงจากฐานเดิม) เพียงแค่ต้องสลับค่าสีให้ตรง token ใหม่ ไม่ใช่ redesign layout ใหม่ทั้งหมด — เรียก UIUX ทำงานซ้ำที่ไม่จำเป็นจะเสียเวลาโดยไม่ได้ประโยชน์เพิ่ม
- ต้องอัป requirements: ไม่ต้อง (ตรงกับ AC ที่เขียนไว้แล้ว เป็นการตีความ AC ที่ชัดเจนอยู่แล้ว ไม่ใช่การเปลี่ยน scope)

### ประเด็น 28 (รอบ 18): US-38 AC4 (ผู้ใช้ยืนยันด้วยตาจริง) ไม่ใช่สิ่งที่ QA อัตโนมัติ (subagent) ตัดสินแทนได้
- คำตัดสิน: T144 เป็น manual step ที่ orchestrator ต้องขอให้ผู้ใช้จริงยืนยันก่อนปิด epic ทั้งหมด (US-35 ถึง US-38) — QA subagent (automated) ตัดสิน PASS ได้เฉพาะ T139-T143 (regression + checklist + hex audit) เท่านั้น ห้ามตัดสิน AC4 แทนผู้ใช้
- เหตุผล: สอดคล้องกับ pattern เดิมของโปรเจกต์ (T30 perf บนอุปกรณ์จริง, US-31 AC2 scroll หลังปิด in-app browser) ที่ QA เคยยอมรับว่าเป็นข้อจำกัดของเครื่องมือทดสอบอัตโนมัติ ต้องมี manual confirmation แยกต่างหาก
- ต้องอัป requirements: ไม่ต้อง (ตรงกับ AC เดิมที่เขียนไว้ชัดแล้วว่าต้องเป็นผู้ใช้ยืนยัน ไม่ใช่ QA อัตโนมัติ)

### ประเด็น 26 (รอบ 15): ระดับความลึกของการปรับ Map3D — สี/แสงเงา vs geometry/สไตล์เรนเดอร์
- คำตัดสิน: เฟสนี้ปรับเฉพาะสี/แสงเงา/material ของตึกและพื้นบนโครงสร้าง geometry เดิม (T128) — **ไม่แตะ geometry, จำนวน tile, มุมกล้อง/perspective**
- เหตุผล: ความเสี่ยงต่ำกว่ามาก ไม่กระทบ test ที่ผูกกับจำนวน/โครงสร้าง tile เดิม (US-1 ต้อง render ครบ 76 จังหวัดเสมอ) และตรงกับ Out of Scope ที่ BA ระบุไว้แล้วว่าเป็นค่าเริ่มต้นของรอบนี้ ถ้าหลัง QA เห็นว่าสี/แสงเงาอย่างเดียวไม่พอให้รู้สึก "advance ขึ้นจริง" ค่อยกลับมาพิจารณาขยับ geometry เป็นรอบถัดไป
- ต้องอัป requirements: ไม่ต้อง (ตรงกับ Out of Scope เดิมของ requirements.md ที่เขียนไว้แล้วว่า "ค่าเริ่มต้นของรอบนี้คือปรับสี/แสงเงา/gradient บนโครงสร้างเดิม" — เป็นการยืนยันไม่ใช่เปลี่ยน scope)

### T125 — ผลตัดสินใจ (Decision Gate ปิดแล้ว)
ผู้ใช้ยืนยันเลือก **ตัวเลือก A: "Deep Jade"** (evolve จากเขียวเดิม #1D9E75/#0F6E56 → เขียวมรกตเข้ม/อิ่มตัวขึ้น #15A87A/#0B5C46, พื้นหลังขาวอมมินท์ #F6F9F7) — ดูตารางเต็มใน `docs/design-spec.md` หัวข้อ "ตัวเลือก A: Deep Jade"

**ประเด็น `mapCanvasBg` ที่ UIUX ส่งมาให้ตัดสินใจคู่กัน**: เลือก **ทางเลือก B (tint อ่อนของพื้นหลังหลัก ไม่ใช่พื้นเข้ม)** ตามคำแนะนำของ UIUX เองที่ผูกเงื่อนไขไว้แล้วว่า "ถ้าเลือกโทน A แนะนำทางเลือก B" — เหตุผล: พื้นเข้มจะขัดกับโทนอ่อนทั้งระบบของ Deep Jade และเพิ่มความเสี่ยง contrast โดยไม่จำเป็น ไม่ต้องอัป requirements (อยู่ในกรอบที่ UIUX เสนอไว้แล้ว)

**T126 ถึง T131 ปลดล็อกแล้ว — ส่งต่อ programmer implement ได้ทันที**
</content>
